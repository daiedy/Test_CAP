# Changelog

Format: date, area, what changed. Maintained by `docs-keeper`, all agents add to it. New entries on top.

## 2026-09-25

- test: **`products-rating-column` phase 4 (#5), OPA5.** New `app/products/webapp/test/integration/RatingShownAsStarsJourney.js` (3 `opaTest`s: "The list report shows the rating column as stars" with `iCheckColumns(5, ...)` and `iCheckCells` on the `Laptop Pro 15` row, "The object page shows the rating in General Information" with `iCheckField`, "Teardown"), registered in `opaTests.qunit.js` before `DraftMarkerInListReportJourney`; `RussianLocaleJourney.js` +1 `ru` header assertion of the Rating column; new `data/RatingTexts.js` (column key, field identifier, `en`/`ru` label as `\u` escapes). Keys measured on the running app (FE 1.152.0): the column's property key is `rating` (id `...::C::DataPoint::Rating`), the form field is `{ property: 'DataPoint::Rating' }` (`FormElement::DataFieldForAnnotation::DataPoint::Rating`; the `targetAnnotation` of the `FieldIdentifier` typedef is not read by `BaseAPI`); the cell is asserted with the `editor` state, which unwraps the FE wrappers down to the `sap.m.RatingIndicator`. `npm run test:ui` against `cds serve`: 28/28.
- test: **`products-rating-column` phase 3 (#5), contract part.** `test/metadata.test.js` +1 "exposes the rating column as a UI.DataPoint with Visualization Rating" (compact `$metadata` comparison: the `UI.DataPoint #Rating` record, the `DataFieldForAnnotation` with `UI.Importance #Low` inside `UI.LineItem`, the same record without importance inside `UI.FieldGroup #GeneralInfo`; proven red on the phase 2 model in a scratchpad copy). Deliberate contract change: `test/__snapshots__/metadata.test.js.snap` updated with `npx vitest -u` because the rating column annotations enter the OData contract; raw git diff +16/-0 (the plan expected raw +72/-56 with a positional move, git shows none), `app/products/webapp/localService/metadata.xml` regenerated (same +16/-0). Backend suite 69 tests.
- app: `products-rating-column` phase 3 (#5), annotations part: `UI.DataPoint #Rating` (`Value: rating`, `TargetValue: 5`, `Visualization: #Rating`, no Title/Description) in `app/products/annotations/Products.cds`, referenced by a `UI.DataFieldForAnnotation` with `Label: '{i18n>Products.rating}'` as the 5th `UI.LineItem` column (`UI.Importance: #Low`) and as the 4th field of `UI.FieldGroup #GeneralInfo` (after Category, before Image URL); `rating` added to the 15 records of `localService/mockdata/Products.json` (values from the CSV). `manifest.json` unchanged, no fragment. Contract test, snapshot and `metadata.xml` follow in the same phase (`test-backend`).
- pipeline: `npm run lint` was red since ff1dfa9: ESLint `no-control-regex` flagged the intended ESC (`\x1b`) in the ANSI-stripping regex of `scripts/hooks/session-start.mjs`. An `eslint-disable-next-line` comment with the reason fixes it (protected file, edited on the user's explicit approval). It slipped through because the file was edited via Bash, which the PostToolUse ESLint check does not cover, the Stop gate does not run lint, and the commit was not pushed to CI.
- feature `products-rating-column` (#5), phase 2 in progress: `rating : Integer` on `my.catalog.Products` (after `stock`), seeded for the 15 products in the CSV, `@title` and `@assert.range: [0, 5]` in `srv/annotations/Products.cds`, `Products.rating` in `_i18n` en and ru. Tests and the contract snapshot follow in the same phase. 
- test: **`products-rating-column` phase 2 (#5).** `test/catalog-service.test.js` +4: "returns the seeded rating of a product", "rejects a rating above 5 (@assert.range)", "rejects a negative rating (@assert.range)" (active `PATCH` of `Yoga Mat`, 400 `ASSERT_RANGE`, target ends with `rating`, stored 4 kept) and "rejects activation of a draft with a rating above 5 (@assert.range)" (draft `PATCH` 200 with an `ASSERT_RANGE` `DraftMessages` entry, `draftActivate` 400). Deliberate contract change: `test/__snapshots__/metadata.test.js.snap` updated with `npx vitest -u` (+6/-0: `Property rating Edm.Int32`, `Common.Label`, `Validation.Minimum` 0, `Validation.Maximum` 5) because the new element `Products.rating` and its `@assert.range: [0, 5]` enter the OData contract; `app/products/webapp/localService/metadata.xml` regenerated (same +6/-0). Backend suite 68 tests.
- pipeline: **the session briefing is visible to the user (chore `briefing-visible`).** The SessionStart hook wrote plain text to stdout, which Claude Code adds to the model's context but never shows in the terminal, so the ADR-0019 briefing reached the model only. `scripts/hooks/session-start.mjs` now emits JSON: `systemMessage` shows the briefing (and the environment line when it warns) to the user, `hookSpecificOutput.additionalContext` carries the full text (briefing, STATE `Now` and `Open debt`, upstream pointer, environment, protocol) to Claude. Also fixed the false `global cds not found` warning: `cds --version` colors its output even when piped and the ANSI codes broke the version regex; the hook strips them first (`@sap/cds-dk` 10.0.7 is installed). Takes effect after a session restart, the hook is snapshotted at start.
- pipeline: **backlog in GitHub Issues (ADR-0019, accepted by the user; chore `backlog-issues`).** The user's task list of six features first became `docs/features/<name>/REQUEST.md` folders in this same session and was rejected: planned material must not live in the repository, the queue needs priorities, and the session must open with a briefing in the user's language. Now a planned feature is an issue with the `feature` label and a `prio:P1..P3` label (`.github/ISSUE_TEMPLATE/feature.yml` is the shape; the six features are issues #5 to #10, all P2, #6 blocked by #5, #9 blocked by #8). New skill `/backlog` (queue, create from a description, priority, blockers), `scripts/lib/backlog.mjs` (queue by priority then age, blockers from the "Blocked by" section, recommendation, cache `.pipeline/issues.json`), CLI `scripts/backlog.mjs` (`list`, `briefing`, `create`, `prio`, `blocked-by`, `status`, `close`, `setup-labels`), `scripts/prune-feature.mjs` (a finished feature keeps `SUMMARY.md` with a `## Full record` permalink). `/spec` and `/feature` accept `#N`, set `spec-ready` and `in-progress`, phase 7 closes the issue with the summary and prunes the folder. The four finished features are pruned the same way (full records at the permalinks in their `SUMMARY.md`). SessionStart prints the briefing first (language, now, queue, recommendation, debt count) and the upstream digest as a pointer. Language setting `PIPELINE_LANG` (`env` in `.claude/settings.local.json`, read by the scripts, printed by the briefing so the model sees it; texts in `scripts/i18n/pipeline.properties` and `pipeline_ru.properties`); CLAUDE.md invariants 2 and 10, doc map, skill table and do-not list; CONVENTIONS "Languages", PATTERNS "Infrastructure" (three rows), STACK steps 7 and 8. `test/backlog.test.js` (7 tests) pins the queue, the recommendation and both bundles; `test/prompt-budget.json` re-recorded for the grown CLAUDE.md, `spec` and `feature` skills and the new `backlog` skill (retro decision folded into this chore).

## 2026-09-24

- pipeline: context budget by structure, not by line counts (ADR-0018, accepted by the user; chore `context-budget`). Measured: the SessionStart output shrank from 18.1 KB to 6.0 KB. `docs/STATE.md` follows the new `templates/STATE.md` (`## Now` with six labeled lines, `## Open debt` rows, `## What works` items, `## Decisions` pointer); the narrative of 2026-09-07 to 2026-09-23 stays in this file and in the feature `SUMMARY.md` files, the old text is in git history. New `scripts/lib/doc-shapes.mjs` (`stateShapeErrors`, `planShapeErrors`, `contextShapeErrors`, `capped`, `STATE_PRINT_BUDGET` 4096 bytes as a ratchet); `file-checks.mjs` reports a shape violation on PostToolUse, `stop-gate.mjs` blocks on it; `session-start.mjs` prints `Now` and `Open debt` and the latest digest by heading with a visible truncation marker (`readSection` in `hook-utils.mjs` replaces `readLines`); `pre-compact.mjs` writes to `.pipeline/sessions.log` instead of STATE. Feature documents split by audience: `templates/feature/CONTEXT.md` is the implementers' brief, new `SCREENS.md` for `ux-designer`, experiments go to `research/`; `PLAN.md` holds one criterion per line and steps as table rows; new `scripts/check-feature-docs.mjs` runs at the phase 1 gate (`feature`, `spec` skills). Prompts and rules: 12 dated stories rewritten as rules with `(CHANGELOG YYYY-MM-DD)` pointers, MCP routing kept once in protocol section 3 (CLAUDE.md invariant 1 and five rules now point there), reading list per role in the `feature` skill, `reviewer` step 8 points to LESSONS and PATTERNS instead of the plan, `docs-keeper` and `reviewer` read only the top date section of this file. New `scripts/prompt-budget.mjs --record` pins the byte size of every prompt, rule and skill in `test/prompt-budget.json` (35 files, 103,943 bytes); `test/prompt-budget.test.js` (3 tests) fails on growth or a dated story, `test/doc-shapes.test.js` (9 tests) pins the shapes; backend suite 57 tests. `retro` skill: rule form for prompt lessons, agent-memory expiry step, LESSONS by shape. Two stale agent-memory topics deleted (registry deadlock and `.stale` marker, both closed by ADR-0017).
- docs: `docs/decisions/ADR-0018-context-budget.md` (accepted), `templates/STATE.md`, `templates/feature/SCREENS.md`.

## 2026-09-23

### pipeline
- Retro of `catalog-authorization` (`/retro`, on the user's request). Facts: four blocks of the registry gate (two agents at SubagentStop, the orchestrator twice at Stop), four agents stopped by the turn limit or the 600 s stream watchdog (`ux-designer`, `test-ui`, `ui-verifier`, `reviewer`; only work already on disk survived, the reviewer's 53 tool calls left nothing), three defects found by verification rather than by the authoring agent (the `+50` EDMX figure, the one-line contract assertion, the mock fixture shape - the last one introduced by the orchestrator "fixing" a correct fixture on a `curl GET`), MCP audit clean (112 `mcp` and 42 `edit` records over nine agent types, zero `justification` or `skipped-unjustified`, `MCP_RULES` unchanged).
- **ADR-0017, generated files are sanctioned by their content, not by the write route.** `scripts/lib/protected-paths.mjs` gains `GENERATED`, `GENERATED_NO_HEADER` and `protectedWriteHit(root, r)`; `subagent-stop.mjs` and `stop-gate.mjs` call it instead of `protectedHit()`, so the output of `npm run docs:registry` (no `edit` record by definition) passes while a registry file without the generator header still blocks; `docs/registry/.stale` joins `PROTECTED_EXCEPTIONS` and `.gitignore`; the SubagentStop message stops advising `git checkout`, which `protect-files-bash.mjs` denies to a subagent, and asks for a report instead. Pinned by `test/hooks-registry-gate.test.js` (7 tests); rule `pipeline-config.md` documents the exemption; protocol section 9 forbids reverting or deleting a file to satisfy a gate.
- Work lost at limits: protocol section 2 gains item 10 (a document deliverable exists on disk before the research and grows as you go; stop cleanly and name the last completed item); `reviewer` creates `docs/features/<name>/REVIEW.md` after the diff and appends findings as it goes; `test-ui` verifies each artifact as soon as it exists and runs against `cds serve`, not `cds watch` (its prompt contradicted rule `tests-ui.md`); `ui-verifier` starts `cds serve` too; the `feature` skill tells the orchestrator to read `git status` and resume a stalled agent with SendMessage before relaunching.
- Verify, do not fix: the `feature` skill forbids the orchestrator to fix an agent's output; a defect goes back to the owning agent with the evidence, and an unavoidable own check uses the check the acceptance criterion names (the browser or the framework's `$batch` shape, not a hand-typed request). Rule `ui5-webapp.md` now also covers `app/**/webapp/localService/**` and states that every mockdata file is a JSON array, singleton included; new PostToolUse check `checkMockdata` in `scripts/lib/file-checks.mjs` flags an object fixture.
- Smaller transfers: `architect` labels a scratchpad figure with the phase it describes; `ui-verifier` and PATTERNS "Role-aware UI visibility" require checking a visibility mechanism per page (one unresolved `$Not` defaulted to visible on the List Report and hidden on the Object Page); `templates/feature/PLAN.md` and `VERIFICATION.md` phrase the console criterion as "no message new against the latest `VERIFICATION.md` on `main`" instead of forbidden substrings (the old form tripped on a pre-existing FE i18n assert); the Cyrillic check (`file-checks.mjs`), protocol section 4 and CLAUDE.md invariant 10 now agree that rendered UI quoted as evidence in `VERIFICATION.md` is allowed, as every verification report on `main` already did.
- `docs/LESSONS.md`: three pending entries transferred, zero pending; the UI5 1.152.0 singleton-path console error (root cause by `reviewer`) added as the fifth `Pending upstream`; the 2026-09-07 "transferred" table deleted after two releases, the file is down to 19 lines. History only, no transfer: stopping a background Bash task with TaskStop also skips its trailing restore command (restore by hand); `docs-keeper` flagged two genuine harness notices (a memory file changed, a session restart) as possible injected content and, correctly, did not act on them.

- **CI hotfix (`chore/ci-auth-probe`).** Both merges of the day were red at the OPA5 job's "Start CAP server" step: the readiness probe `curl -sf 'http://localhost:4004/odata/v4/catalog/$metadata'` has received 401 since ADR-0013 (`@requires: 'authenticated-user'`), `-f` treats 401 as a failure, the 30 s loop ran out and the journeys were skipped while the server log showed a healthy start in 588 ms. The probe now authenticates as the mocked editor (`-u alice:`), as do the smoke curls of the `run-app` and `test-all` skills and the `ui-verifier` server check (which would have read 401 as "down" and started a second server). Cause: the feature plan asserted `.github/workflows/ci.yml` unchanged and the review verified exactly that assertion; `architect` now lists every caller outside the application when a change alters what an endpoint answers to an anonymous request. The orchestrator also misreported both runs as green by reading the exit code of a trailing `echo` instead of `gh run watch --exit-status`; the run log itself said `exit=1`.

### test
- New `test/hooks-registry-gate.test.js` (7 tests): generator output of `docs/registry` passes the git-based gates, a header-less registry file blocks, `.stale` is exempt, every other protected path is audited unchanged; the committed array fixture `mockdata/Permissions.json` passes `checkMockdata` and an object fixture is flagged. Backend suite: **45 passed** (was 38).

### docs
- ADR-0017 `docs/decisions/ADR-0017-generated-files-gate.md` (proposed at retro time, accepted by the user before the merge). CLAUDE.md invariant 10 wording; `templates/feature/PLAN.md` step 6 and `templates/feature/VERIFICATION.md` "Browser console"; PATTERNS row "Role-aware UI visibility" (array fixture, per-page check); `.gitignore` hides `docs/registry/.stale`; STATE "Where we are" and "Next steps".

## 2026-09-16

### docs
- Feature `catalog-authorization`, plan step 13 (`ui-verifier`, `d3a4db0`): `VERIFICATION.md` and 11 screenshots for 7 scenarios plus V10 (keyboard tab order). Blocking viewer criterion (fresh browser instance, no Create/Delete/Edit, `Permissions` returns `isEditor: false`) **PASSED**. One defect found and fixed in the same commit: the mock fixture needed the array form, not the object form (`app/products/webapp/localService/mockdata/Permissions.json`), because `@sap-ux/fe-mockserver-core` throws on the object form for the `$batch`/`$select` path FE actually uses. One observation carried to `reviewer`: a reproducible `Failed to read path .../Permissions/isEditor` console error on :4004 for both roles.
- Feature `catalog-authorization`, plan step 14 (`reviewer`, `94b5883`): `REVIEW.md`, zero blocking findings over five commits. Root-caused the verifier's console observation to an unguarded UI5 1.152.0 defect in `_Helper.aggregateExpandSelect` (a container-qualified `$edmJson` `$Path` is valid FE-converted metadata but not a valid OData V4 data-binding path) and judged it cosmetic, not blocking. Five non-blocking findings handed to `docs-keeper`/`test-backend` for phase 6 (a stale test comment, a refuted STATE claim already fixed by the orchestrator, a stale "37 tests" figure, missing CHANGELOG lines, and the console criterion needing an honest "not met" record).
- Feature `catalog-authorization`, plan step 15 (`docs-keeper`, phase 6): registry regenerated and confirmed fresh; `docs/STATE.md`, `docs/architecture/PATTERNS.md` ("Authorization" row extended, new row "Role-aware UI visibility"), `CONVENTIONS.md` section 2 (permission-singleton layering sentence), `TESTING.md` rule 5 (reworded for `defaults.auth` scope and the 401/403 shapes), `ARCHITECTURE.md` (Basic-auth sentence, new "Roles" table, `Permissions` paragraph), `README.md` (Basic prompt sentence), `templates/service.test.js` (anonymous + denied-role tests) updated per ADR-0013's consequences; `docs/features/catalog-authorization/SUMMARY.md` written; `PLAN.md` fully ticked with the console criterion recorded as "not met, accepted with rationale"; ADR-0013 consequences ticked; `docs/LESSONS.md` folded in the two pending entries and gained the mock-fixture and silent-hiding-asymmetry lessons of this feature.

## 2026-09-11

### docs
- Feature `catalog-authorization`, plan steps 1-3 (`architect`/`ux-designer`, phase 1, commit `3fdd195`): `docs/features/catalog-authorization/CONTEXT.md` and `PLAN.md` rewritten after the user's nine decisions of 2026-09-10 (`CatalogViewer`/`CatalogEditor` roles, the permission-singleton UI mechanism); `docs/decisions/ADR-0013-catalog-authorization.md` accepted (user, 2026-09-10), covering authentication, the two roles, annotation placement, development users, test idioms, the UI credential mechanism, the role-aware UI singleton and handler, and the `xs-security.json` deferral. `CONTEXT.md` "Screens" section (`ux-designer`) records the List Report/Object Page baseline, seven UX risks (R1-R7) and thirteen verifier scenarios (V1-V13) for the hide-versus-disable decision, with the conclusion that no new i18n key is needed.

### srv
- Feature `catalog-authorization`, plan steps 4-6 (`cap-backend-dev`, phase 2): `srv/catalog-service.cds` gains `@requires: 'authenticated-user'` on `CatalogService` and `@restrict: [{ grant: 'READ', to: 'CatalogViewer' }, { grant: '*', to: 'CatalogEditor' }]` on the `Products` projection next to `@odata.draft.enabled` (ADR-0013 parts 1-5); `Categories` stays `@readonly` without a `@restrict`, so code lists and `$metadata` remain readable by any authenticated user. New read-only singleton `CatalogService.Permissions` (`@odata.singleton @cds.persistence.skip @readonly`, `key ID : String`, `isEditor : Boolean`) as the permission signal for the UI. `package.json` gains `cds.requires.auth.users` with `alice` and `bob` as `CatalogEditor` and the new user `viewer` as `CatalogViewer` (no passwords, defaults and `"*": true` kept; `cds env requires.auth.kind` still prints `mocked`) - the only change in that file, and the lockfiles are untouched.
- First handler of the project: `srv/catalog-service.js` (ESM, `extends cds.ApplicationService`), exactly one registration `this.on('READ', 'Permissions', ...)` replying `{ ID: 'me', isEditor: req.user.is('CatalogEditor') }` (ADR-0013 part 8). Enforcement stays fully declarative; `req.user.is` serves the UI signal only. Measured against `cds serve --in-memory`: `GET /Permissions` 200 with `isEditor: true` for `alice` and `false` for `viewer`, 401 anonymous, `PATCH` 405 `ENTITY_IS_READ_ONLY`, `GET /Products` 401 anonymous, `POST /Products` as `viewer` 403.
- OData contract changed in this phase: `app/products/webapp/localService/metadata.xml` and `test/__snapshots__/metadata.test.js.snap` regenerated (`npx vitest -u`), **+20 lines, 0 removed** - the `<Singleton Name="Permissions"/>` in the entity container, the `Permissions` `EntityType` with `ID` and `isEditor`, and the `Capabilities.Delete/UpdateRestrictions: false` block derived from `@readonly`. The architect's measured +50 covers the whole feature; the remaining 30 lines are the three `UI.*Hidden` blocks, emitted twice each, which arrive in phase 3.

### app
- Feature `catalog-authorization`, plan step 9 (`fiori-app-dev`, phase 3): `app/products/annotations/Products.cds` gains a second `annotate CatalogService.Products` block with `UI.CreateHidden`, `UI.UpdateHidden` and `UI.DeleteHidden`, each `{ $edmJson: { $Not: { $Path: '/CatalogService.EntityContainer/Permissions/isEditor' } } }` (ADR-0013 part 8). This hides the four standard FE V4 actions from a `CatalogViewer`: Create in the List Report toolbar, Delete on row selection, Edit and Delete in the Object Page header. The annotations stay in the presentation layer per ADR-0004; the `isEditor` flag they read is service API in `srv/catalog-service.cds`. No `manifest.json` change, so no Fiori MCP `execute_functionality` run, and no new i18n key (a hidden button renders no label).
- OData contract regenerated in the same step: `app/products/webapp/localService/metadata.xml` and `test/__snapshots__/metadata.test.js.snap` (`npx vitest -u`), **+30 lines, 0 removed** in this phase and **+50 lines, 0 removed** against `main` for the whole feature, as planned. Each term is emitted twice, on `CatalogService.Products` and on `CatalogService.EntityContainer/Products` (measured: 2 occurrences per term), and the compiler pretty-prints the expression as `<Annotation Term="UI.CreateHidden"><Not><Path>...</Path></Not></Annotation>` across four lines.

### test
- Feature `catalog-authorization`, plan step 7 (`test-backend`, phase 2): `test/metadata.test.js` sets `test.defaults.auth = { username: 'alice' }` right after `cds.test(...)` - `@requires` on `CatalogService` also protects `$metadata`, and without the line the two HTTP contract tests failed with 401. New contract test "exposes the Permissions singleton" (`<Singleton Name="Permissions"` in the served EDMX; measured 0 occurrences before the feature, 1 now). The second half of the contract criterion, the three `UI.*Hidden` annotations with `<Not><Path>/CatalogService.EntityContainer/Permissions/isEditor</Path></Not>`, is written but `it.skip`ped until they exist (plan step 9); measured 0 occurrences today and 2 per term against a scratchpad copy carrying the phase-3 annotation, where the un-skipped test passes. Its assertion compares the EDMX with the whitespace between tags removed, because the compiler pretty-prints `<Not>` and `<Path>` onto separate lines.
- `test/catalog-service.test.js` gains the helpers `as(username)` / `anonymous` and a final `describe('CatalogService authorization')` with seven tests: anonymous 401 on `Products` and on `$metadata` (numeric `status`, not a string `code`); a `CatalogViewer` reading products, categories, currencies and `$metadata`; a viewer denied `POST` both as an active record and as a draft, with a follow-up check as `alice` that no `Test Lamp` exists in either form; a viewer denied `PATCH`, `DELETE` and `draftEdit`, with the seeded Yoga Mat unchanged and draft-free afterwards; `carol` (authenticated, no catalog role) denied on `Products` but allowed on `Categories`; the `Permissions` singleton reporting `isEditor` true for `alice`/`bob`, false for `viewer`/`carol` and 401 anonymous; `PATCH /Permissions` 405. The 19 existing tests are unchanged and still green, including the `bob` draft lock (409 `DRAFT_ALREADY_EXISTS`) and "does not allow creating categories" (405, not 403). `test/hooks-protect-bash.test.js` is untouched and deliberately sets no `defaults.auth`: it starts no server. Suite: **37 passed, 1 skipped (38)**.

- Feature `catalog-authorization`, plan step 9 (`fiori-app-dev`, phase 3): the `.skip` is removed from "hides the editing actions of Products from anyone who is not a CatalogEditor" in `test/metadata.test.js`, the only change to that file; its assertion was already complete. Suite: **38 passed, 0 skipped**.

- Feature `catalog-authorization`, plan steps 10-11 (`test-ui` started them, the orchestrator verified and finished them after the agent stalled; phase 3): new `app/products/ui5-test-runner.json` with `{ "browserArgs": ["--basic-auth-username", "alice"] }`. `ui5-test-runner` appends `browserArgs` after `--` to the puppeteer script, which calls `page.authenticate`, so the headless browser answers the Basic challenge of both the test page and the app iframe. `scripts.test:ui`, `app/products/package.json` and `.github/workflows/ci.yml` are unchanged: CI runs `npm run test:ui -- --report-dir ...`, and credentials placed after `--` in the script would be swallowed as browser arguments.
- New OPA5 journey `app/products/webapp/test/integration/RoleAwareActionsJourney.js`, registered last in `opaTests.qunit.js`. It is the regression guard for plan risk R6: if the `$edmJson` path ever stops resolving, the annotations evaluate to "hidden" and the four actions disappear for editors too, silently and with no console error. Case 1 asserts, as `alice` (a `CatalogEditor`), that Create is visible and enabled, that Delete is visible before and enabled after a row selection, and that the Object Page header shows Edit and Delete; case 2 is teardown only. The suite authenticates as one user per run, so the viewer half is covered by the backend tests and the blocking `ui-verifier` criteria, per the user's decision of 2026-09-10. Suite: **25 opaTests, 0 skipped**, run against `npx cds serve --in-memory --port 4004`.
- New mock fixture `app/products/webapp/localService/mockdata/Permissions.json` = `[{ "ID": "me", "isEditor": true }]` (**array form**). The shape matters, and it was settled the wrong way first: a plain top-level `curl GET /Permissions` returns an empty body for the array form and the correct object for the object form, which is why the object form was committed in `4e443e1`. That test used the wrong path. FE V4 never issues a top-level GET for the singleton - it asks for `GET Permissions?$select=isEditor` **inside a `$batch`**, and on that path the object form makes `@sap-ux/fe-mockserver-core` throw `tenantJsonData.forEach is not a function` in `entitySet.js:getInitialDataSet` on every call, while the array form answers 200 with `{"ID":"me","isEditor":true}` and logs nothing. Measured both ways, cold and warm. Visible consequence of the object form in `npm run start-mock` (found by `ui-verifier`, scenario 6): the List Report kept Create and Delete but the Object Page lost Edit - the same unresolved `$Not` expression defaulting in opposite directions on the two pages. With the array form both pages are correct: 15 products, Create and Delete in the List Report, Edit and Delete in the Object Page header, verified in a browser.

## 2026-09-10

### pipeline
- Bash write gap closed (ADR-0016, user decision "C + A" on 2026-09-10). The gates no longer trust the tool that wrote a file. New `scripts/lib/file-checks.mjs` holds the per-file-type dispatch that used to live inside `post-edit.mjs` (which shrinks from 306 to 41 lines); new `scripts/lib/protected-paths.mjs` holds the protected list once, for all hooks. `subagent-stop.mjs` now diffs the changed files against the ADR-0014 `edit` records, runs those checks over everything written outside Edit/Write (capped at 12 files, and it says when it skipped some), and blocks with exit 2 on a protected path; it also lints `scripts/**/*.mjs`, which it did not before. `stop-gate.mjs` repeats the protected-path audit for the main thread. New PreToolUse hook `protect-files-bash.mjs` (matcher `Bash`) catches the ordinary accident earlier: `deny` for a subagent, so an unattended `/feature` run never stalls on a prompt, and `ask` for the main thread, so the user approves a deliberate maintenance edit in the moment. `scripts/lib/**` joins the protected list because the checks now live there. Rule `pipeline-config.md` updated and its `paths:` extended.
- Two things were learned by building it, both recorded in ADR-0016. First, a `chore/*` branch was tried as a second sanction next to `PIPELINE_ALLOW_PROTECTED=1` and removed: a branch name is not a boundary, any agent can create one. Second, the first version of the guardrail locked the session out of its own remaining fixes, because Edit/Write, Bash and the environment variable were all closed at once (`VAR=1 <command>` reaches that command, not the hook, which reads the Claude Code process environment). The way out is the documented one: the user restarts the session with `PIPELINE_ALLOW_PROTECTED=1`, which is exactly the property wanted - the switch is not the agent's to flip. The `ask` decision for the main thread removes the need for a restart next time.
- The new gate paid for itself on its first run: it caught four `no-useless-escape` errors and one unused constant in the very hook being added, all in files that the old pipeline would not have linted.

- Retro of `products-draft-marker` (`/retro`, on the user's request): six pending LESSONS entries triaged, five transferred, one left for the user's decision. Destinations: rule `tests-ui.md` gains the `iNavigateByBreadcrumb` trap with its teardown/restart fallback and the fact that `@Common.SemanticKey` does not invalidate existing `iCheckRows`/`iPressRow` assertions (`sap/fe/test/builder/MacroFieldBuilder.js` matches `sap.m.ObjectIdentifier` on `title`) together with the `isDraft` matcher; rule `i18n.md` gains "texts a control renders itself are framework texts, not project keys" (`sap.m` `OM_*`, `sap.fe` `C_TRANSACTION_HELPER_*`); rule `ui-annotations.md` gains the `@Common.SemanticKey` requirement for draft-enabled entities (the `UI.HeaderInfo` editing-status fallback is OData V2 only); `PATTERNS.md` row "Draft and lock marker in the List Report row" gains the test consequence. Agent memory `architect/fe-v4-semantic-key-marker.md` corrected at its source: the breadcrumb clause now carries the measured "passes without navigating" caveat, and its "icon only below 600 px" claim is marked disproved inside an FE V4 table cell.
- UI test run: `npm run test:ui` runs against `npx cds serve --in-memory --port 4004`, the way `.github/workflows/ci.yml` starts the server, not against `cds watch`. Measured 2026-09-10: against a long-running `cds watch` the runner registered no test page at all (`testPageUrls: []`, "No test page found (or all filtered out)"), and against a second, concurrent `cds watch` the puppeteer probe navigation hung for 20 minutes and failed; the same tree ran 23/23 in 68 s against `cds serve`. Rule `tests-ui.md` and `docs/architecture/TESTING.md` updated. CI was never affected (run 34394169431 green) because it already uses `cds serve`.
- OPA5 page timeout, measured instead of guessed: `app/products/package.json` `test:ui` becomes `--split-opa --page-timeout 150000 --global-timeout 900000` (it was one page with `--page-timeout 900000`). Measurements: 23 tests on a single page take 85 s in CI (job "OPA5 journeys", step "Run OPA5 journeys with ui5-test-runner") and 68 s locally; with `--split-opa` the five journey pages take 10/7/22/10/24 s for a 91 s total. `--page-timeout` is per page, wall-clock, and has no retry (`pageTimedOut()` fails every test of the page that has not reported), which is why the 2026-09-09 freeze cost 15 minutes and failed the whole 9-test tail; one journey page now fails after at most 150 s (6x the slowest measured page) and the other four still run. Validated locally against `cds serve`: 4+3+6+4+6 = 23 green, exit 0.
- MCP audit log of the marker run read for the first time (ADR-0014, `/retro` step 1): 267 records, 170 `mcp` and 97 `edit`, 13 agent instances of 9 types, and zero `justification` / `skipped-unjustified` records - the gate never fired once, so it produced no retro material. No `MCP_RULES` row forced a pointless query and no edited file type lacks a rule (the 27 unruled files are Markdown, where a query is meaningless). The log confirmed the Bash blind spot from the other side: `app/products/annotations/Products.cds` was changed by commit `1287c3c` and carries zero `edit` records.
- `docs/LESSONS.md` down from six pending entries to one: only the Bash write blind spot remains, with its premise corrected (`subagent-stop.mjs` does lint Bash-written files via `changedFiles()`; what is missing for them is `cds compile`, the Cyrillic and i18n scans, the protected-path denial and the ADR-0014 `edit` records) and four costed options for the user to choose from.

### app
- `feature/products-draft-marker` merged into `main` (`bdb6321`, `--no-ff`) and pushed; CI run 34394169431 green (backend, UI lint, OPA5 journeys). Seven merged branches deleted locally.

## 2026-09-09

### app
- Feature `products-draft-marker`, plan steps 3–5 (`fiori-app-dev`/`test-backend`/`test-ui`, phase 3, commit `1287c3c`): `app/products/annotations/Products.cds` gains `Common.SemanticKey: [ name ]` as the first entry of the `annotate CatalogService.Products with @(...)` block (ADR-0015, accepted by the user). `name` is already the first `UI.LineItem` DataField, which is the placement FE V4 requires, so `UI.LineItem` is unchanged; no `manifest.json`, `srv/`, `db/`, `_i18n/` or i18n-bundle change, and no `Common.Text` on `name` (it would turn the semantic-key cell into two lines). Effect in the UI: the List Report row renders the editing status through `sap.m.ObjectMarker` in the `name` column and that cell becomes a `sap.m.ObjectIdentifier` instead of `sap.m.Text`. OData contract change: exactly one new `<Annotation Term="Common.SemanticKey">` with `<PropertyPath>name</PropertyPath>` inside `Annotations Target="CatalogService.Products"`, plus a positional move of the compiler-generated `Common.SideEffects#alwaysFetchMessages` and `Common.Messages` inside that same block; `app/products/webapp/localService/metadata.xml` and `test/__snapshots__/metadata.test.js.snap` regenerated in the same step, so the sync test in `test/metadata.test.js` stays green. `npm test` reports **23** (19 in `test/catalog-service.test.js`, 4 in `test/metadata.test.js` including the new contract assertion). Verified by two independent read-only auditors: order-insensitive comparison of the whole contract file shows 5 added lines and zero removed, `cds compile '*' --to edmx-v4 -s CatalogService -l en` is byte-identical to the committed file, and `grep SemanticKey` finds nothing in `srv/` or `db/` (invariant 5, ADR-0004).

### test
- Feature `products-draft-marker`, plan step 4 (`test-backend`, commit `1287c3c`): `test/metadata.test.js` gains `it('exposes the semantic key of Products in the contract')` in the existing `describe('OData contract of CatalogService')`, asserting `GET $metadata` with `Accept-Language: en` contains `Term="Common.SemanticKey"` followed by `<PropertyPath>name</PropertyPath>`; the assertion could not pass before step 3's annotation. `test/catalog-service.test.js` unchanged.
- Feature `products-draft-marker`, plan step 5 (`test-ui`, commit `1287c3c`): new OPA5 journey `app/products/webapp/test/integration/DraftMarkerInListReportJourney.js`, registered last in `opaTests.qunit.js`, module "Draft marker in the list report", 6 `opaTest` cases (an unchanged row shows no marker; editing the object page commits a draft, using a teardown/restart fallback instead of the breadcrumb because the breadcrumb is unreachable in edit mode; the own draft is marked in the list report row; the Editing Status filter `Own Draft` narrows the list to that row via `sap/fe/test/api/EditState`; discarding the draft removes the marker; teardown). This delivered shape merges the plan's originally separate "row opens in edit mode and can be discarded" / "marker disappears" cases into one and adds the Editing Status case, both recorded in `PLAN.md` and `SUMMARY.md`. `npm run test:ui` in `app/products` against a fresh `npm run watch`: **23 passed, 0 skipped** (17 existing + 6 new), the four existing journeys and their page objects unchanged on disk; after the run 0 drafts remain and `Laptop Pro 15` is back to `category_code: "ELECTRONICS"`.

### pipeline
- Feature `products-draft-marker`, phase 3, process finding: step 3 wrote its three files through Bash redirection instead of Edit/Write. This did not merely skip the PostToolUse per-file-type compile/lint hooks and the SubagentStop edit-vs-MCP comparison — it also bypassed the PreToolUse `protect-files.mjs` denial (the same hook that blocks direct edits of `app/**/webapp/manifest.json`), because that hook only sees Edit/Write tool calls, not Bash redirection into a project file. No protected file was actually touched this time and both MCP queries the plan required are in the ADR-0014 audit log; the auditors re-ran every gate themselves with green results. The prompts of steps 4 and 5 now require Edit/Write for file changes. `docs/LESSONS.md` records it as needing a rule or hook change, which is the user's call because `.claude/**` and `scripts/hooks/**` are protected.
- MCP audit, soft gate (ADR-0014, user decision): new `scripts/lib/mcp-audit.mjs` (rule table `MCP_RULES`, per-session JSONL log in `.pipeline/`) and hook `scripts/hooks/mcp-audit.mjs` on PostToolUse/PostToolUseFailure `^mcp__|^Skill$`; `post-edit.mjs` logs edits per agent; `subagent-stop.mjs` blocks once when an edited file type had no MCP attempt and accepts a `## MCP not used` section (logged for `/retro`, unexplained skips logged as `skipped-unjustified` without a second block); `protect-files.mjs` denies direct edits of `app/**/webapp/manifest.json` (Fiori MCP only); `session-start.mjs` prunes audit files older than 14 days. Protocol section 2 step 5 and the report form (section 8), rule `pipeline-config.md`, skill `retro` step 1, CLAUDE.md invariant 8 updated. Smoke-tested with sample JSON on stdin. `scripts/lib/hook-utils.mjs` `readStdinJson` now reads stdin in a loop and waits on EAGAIN: with `fs.readFileSync(0)` a piped hook input could arrive as `{}` and the hook silently did nothing (seen in the smoke tests; affected every hook).
- UI5 MCP, correction after the restart: a project `.mcp.json` entry does not replace a plugin's bundled server, because the plugin server registers as `plugin:ui5:ui5-mcp-server`; both ran side by side. The bundled server is toggled off in `/mcp` (per machine, `~/.claude.json`); agent allowlists (`fiori-app-dev`, `test-ui`, `ux-designer`, `ui5-freestyle-dev`) keep only `mcp__ui5-mcp-server__*`; CLAUDE.md, STACK.md (setup step 5), rule `pipeline-config.md` and STATE describe the real mechanics.
- UI5 MCP pinned: `.mcp.json` gains `ui5-mcp-server` = `@ui5/mcp-server@0.2.18`, overriding the unpinned `npx -y @ui5/mcp-server` bundled with the `ui5` plugin (the plugin stays for its skills). Rule `pipeline-config.md` records that a plugin pins only its skills. CLAUDE.md, STACK.md, `docs/upstream/UPDATES.md` and STATE say where the pin lives; agent allowlists (`fiori-app-dev`, `test-ui`, `ux-designer`, `ui5-freestyle-dev`) carry both tool prefixes until a session restart confirms the effective one; protocol and `ui5-webapp.md` use the project-server prefix. STACK.md: new section "Setup on a new machine" (Node 22, `npm ci`, `claude plugin marketplace add capire/skills`, plugin installs, restart, Chrome).
- Protected-file follow-ups of the `products-draft-edit` retro applied on the user's request: agent `ui-verifier` writes `VERIFICATION.md` incrementally with a per-scenario turn budget and blurs a field before Save; agent `architect` schedules the snapshot and `metadata.xml` regeneration in the phase of the model change and replaces the whole ADR `Status:` sentence on acceptance; skill `feature` says the same at the phase 1, 2 and 4 gates and resumes a turn-limited `ui-verifier` with SendMessage; protocol section 3 gains the MCP-unavailable fallback (capire, CDN `-dbg.js` sources, "not verified by MCP"); rule `tests-backend.md` gains the `IsActiveEntity` rule for draft-enabled entities (ADR-0012). LESSONS: 0 pending decisions, 4 pending upstream.

## 2026-09-07

### docs
- `catalog-authorization`: specification (`docs/features/catalog-authorization/`) and ADR-0013 (proposed) with the verified behavior of mocked auth and `@restrict` on a draft-enabled entity.
- Feature `products-draft-edit`, step 2 (`ux-designer`): Screens section added to `CONTEXT.md`.
- Feature `products-draft-edit`, step 11 (`docs-keeper`): ADR-0012 accepted (user, 2026-09-07), consequences ticked; `PLAN.md` approved by the user (`6a95811`) and marked done; `VERIFICATION.md` (`ui-verifier`, 10 scenarios plus a lock-by-`bob` check via curl, 12 screenshots, verdict "ready for review"); `reviewer` found zero blocking findings (`PLAN.md` step 10); `CONTEXT.md` corrected (the branch existed by review time; shell/browser Back with a persisted draft change shows an FE V4 "Warning" dialog with Save / Keep Draft / Discard Draft, not a silent keep; the missing List Report draft/lock marker recorded as a verified, escalated outcome, not a defect); `SUMMARY.md` written; `docs/registry` regenerated; `PATTERNS.md` (rows "Drafts", "Service test", "OData contract", "User scenario"), `docs/architecture/TESTING.md` ("cds 10 specifics") and `templates/service.test.js` updated for ADR-0012; `docs/STATE.md` and `docs/LESSONS.md` updated.

### srv
- Feature `products-draft-edit`, step 3: `srv/catalog-service.cds`: `@odata.draft.enabled` on the `Products` projection (ADR-0012). OData contract change: `Products` gets the key part `IsActiveEntity`, properties `HasActiveEntity`, `HasDraftEntity`, `DraftMessages`, navigations `DraftAdministrativeData` (contained, no entity set) and `SiblingEntity`, entity type `DraftAdministrativeData`, bound actions `draftEdit`/`draftActivate`/`draftPrepare`, `Common.DraftRoot` on `EntityContainer/Products`; `Categories` (`@readonly`) and the generated `Common.ValueList` on `category_code` are unchanged. No handlers, no `cds` configuration (cds 10 defaults: lean draft, bypass draft); non-Fiori clients address active data with `IsActiveEntity=true`. The contract snapshot and `localService/metadata.xml` follow in steps 4 and 6.

### test
- Feature `products-draft-edit`, step 7 (`test-ui`): `app/products/webapp/test/integration/EditCategoryOnObjectPageJourney.js` runs again (`opaTest` instead of `opaTest.skip`, header comment rewritten for the draft-enabled `Products`, ADR-0012). Flow on Laptop Pro 15: Edit offered and pressed, Category is a required dropdown of the six names, Furniture saved and shown in the field and the header description, new test "Cancel discards the change" (Kitchen chosen, Cancel, discard confirmation via `iConfirmCancel`, Furniture kept), Electronics restored, teardown last. Helpers `chooseCategory` and `seeSavedCategory`; every change goes through the dropdown (`iOpenValueHelp` + `CategoryDropdown.iSelectItem`) so the draft PATCH is sent before Save or Cancel. `npm run test:ui` against a fresh `npm run watch`: 17/17 passed, 0 skipped (before: 11 passed, 5 skipped), green twice on the same server; seeded data and no draft left behind.
- Feature `products-draft-edit`, step 4: `test/catalog-service.test.js` adapted to the draft-enabled `Products` (ADR-0012): helpers `active()`, `activeKey()`, `draftKey()`; the create test and the three negative POST tests send `IsActiveEntity: true`; "rejects negative stock" patches the active key and asserts `ASSERT_RANGE`. New `describe('CatalogService.Products drafts')` (own active record in `beforeAll`/`afterAll`, every test discards its draft) with six tests: a POST without `IsActiveEntity` creates a draft (`$count` unchanged, 403 `DRAFT_ACTIVE_DELETE_FORBIDDEN_DRAFT_EXISTS` on the active key, discard 204); edit through `draftEdit`/`PATCH`/`draftActivate` with `DraftAdministrativeData.InProcessByUser`; `@assert.target` surfaces as `DraftMessages` (200) on the draft and as 400 on activation; `@mandatory` is enforced on activation only; `bob` gets 409 `DRAFT_ALREADY_EXISTS` on `draftEdit` and on a PATCH of the locked active record; discard leaves the active record unchanged. Snapshot `test/__snapshots__/metadata.test.js.snap` updated once with `npx vitest -u`. OData contract: `Products` is draft-enabled (`IsActiveEntity` key, `HasActiveEntity`, `HasDraftEntity`, `DraftMessages`, `DraftAdministrativeData`, `SiblingEntity`, actions `draftEdit`/`draftActivate`/`draftPrepare`, `Common.DraftRoot`); `Categories` unchanged. 22 tests (19 service, 3 contract).

### app
- Feature `products-draft-edit`, step 4 (pulled forward from step 6 by the orchestrator): `app/products/webapp/localService/metadata.xml` regenerated with `cds compile '*' --to edmx-v4 -s CatalogService -l en`, because the sync test in `test/metadata.test.js` compares it with the compiled model and the phase-2 gate needs `npm test` green. 131 lines added (the draft artifacts listed under `test`), nothing removed; `mockdata/Products.json` and `manifest.json` unchanged.

### pipeline
- Retro of `products-draft-edit` (lesson triage, 9 inbox entries): draft-enabled entity behavior → `docs/architecture/TESTING.md` "cds 10 specifics", PATTERNS "Service test", `templates/service.test.js`; FE V4 Back-navigation dialog and the missing row marker without `Common.SemanticKey` → PATTERNS "Drafts"; contract change (snapshot and `metadata.xml` in the same phase) → `templates/feature/PLAN.md` contract rule; ADR acceptance replaces the whole Status sentence → `templates/adr.md`; incremental `VERIFICATION.md`, turn budget, `$batch` evidence and blur-before-click → `templates/feature/VERIFICATION.md`; registry labelled the contained `DraftAdministrativeData` as CRUD → fixed in `scripts/gen-registry.mjs`; agent memory in phase commits → CONVENTIONS section 7. Pending a user decision: the same rules in the protected agent prompts, the `feature` skill, the protocol and `tests-backend.md` (diffs proposed in the session).
- LESSONS triage: `docs/LESSONS.md` is now an inbox. 21 of 23 entries were transferred: 4 became PostToolUse checks (`checkSandboxHtml`, `checkSandboxConfig`, `checkUi5Yaml`, `checkTemplateNamespace`), 1 a `test-all` step, 9 path rules (`ui-annotations`, `tests-backend`, `tests-ui`, `ui5-webapp`, `srv-services`), 3 agent prompt lines (`ui-verifier`, `fiori-app-dev`), the rest were already covered by tests, ADRs or CHANGELOG. 2 remain pending upstream (UI5 MCP manifest validation, New Sandbox migration). The `retro` skill now assigns a destination to every lesson; the Stop hook warns above 10 entries.


### pipeline
- First CI run on GitHub failed and was fixed (`8ec176b`): root ESLint now ignores `app/**` (the UI app has its own config and node_modules), `app/products/package-lock.json` regenerated. Run 34112716975 green in all three jobs.
- CI: `.github/workflows/ci.yml` with three jobs (backend lint, tests, registry freshness and compile incl. templates; UI ui5lint and ESLint; OPA5 journeys with `ui5-test-runner` against `cds serve`). Dependabot: `.github/dependabot.yml`, weekly, grouped (sap-cap, sap-ui5-fiori, tooling), cooldown 7/14 days, CAP majors excluded (they go through `/upgrade-cds`), GitHub Actions updates included.
- `app/products/eslint.config.mjs` with `@sap-ux/eslint-plugin-fiori-tools`; scripts `lint:js`, `lint:js:fix`. Baseline: 1 warning (the deliberate `setTimeout` in `Component.js`).

### app
- FLP sandbox home shows only the Product Catalog tile: `appconfig/fioriSandboxConfig.json` now declares the application and an empty tile-group list, overriding the SAP demo tiles that the fixed bootstrap path started to load; the duplicate `LaunchPage` block left `flpSandboxConfig.js`; `<title>` placeholder replaced.
- FLP sandbox works through `npm start` on :8080: `id="sap-ushell-bootstrap"` on the bootstrap tag (livereload was picked as the last script and broke the config URL), relative app URL in the sandbox config, inline scripts moved to `test/flpSandboxConfig.js`, `test/flpSandboxInit.js`, `sandboxConfig.js`, `sandboxInit.js` (CSP warnings gone). `Container.createRenderer` calls carry `ui5lint-disable-next-line` until the New Sandbox migration.


### pipeline
- Retro follow-ups: `maxTurns` raised for test-ui, ui-verifier, docs-keeper (80), reviewer (50), architect and test-backend (60); `feature` skill updates STATE after each phase gate; `architect` decides on draft before planning edit UIs; `reviewer` checks the language rule; `test-all` gained a dev-server smoke step and a Cyrillic scan.
- English-only rule: CLAUDE.md invariant 10, `project-protocol` section 4, PostToolUse hook flags Cyrillic outside i18n bundles, `.texts.csv` and tests.
- CLAUDE.md: request-to-skill routing table (which skill for which request, which agents it runs, who starts it).
- Remaining Russian documents translated to English: STATE, CHANGELOG, LESSONS, feature docs of `categories-code-list`, ADR-0010, ADR-0011, agent memories.

### test
- `test/metadata.test.js`: new test that `app/products/webapp/localService/metadata.xml` equals the compiled EDMX of the whole model (16 tests total).


### db, srv
- Feature `categories-code-list`, phase 2, steps 3–6: `db/schema.cds`: new entity `my.catalog.Categories : sap.common.CodeList` with `key code : String(20)` (ADR-0010). `Products.category` moved from `String(50)` to `Association to Categories`; the foreign key `category_code : String(20)` is generated by the compiler; the OData contract changes from `category` (Edm.String) to `category_code` (Edm.String 20) plus the `category` navigation.
- Data: `db/data/my.catalog-Categories.csv` (6 codes `ACCESSORIES`, `ELECTRONICS`, `FURNITURE`, `KITCHEN`, `SPORTS`, `STATIONERY`, en names, alphabetical order) and `my.catalog-Categories.texts.csv` (6 rows, locale `ru` only); in `my.catalog-Products.csv` the `category` column is renamed to `category_code`, the values are replaced with codes, IDs and the other columns are unchanged.
- `srv/catalog-service.cds`: explicit projection `@readonly entity Categories` (needed for `@assert.target` and titles). `srv/annotations/Products.cds`: `@assert.target` on `category`. New `srv/annotations/Categories.cds`: titles of `code`/`name`/`descr`; keys `Categories.code`, `Categories.name`, `Categories.descr` in `_i18n/i18n.properties` and `i18n_ru.properties`. No handlers. The OData contract change and the `test/__snapshots__` snapshot are recorded by `test-backend` (step 7).
- `Products` uses the `cuid` aspect instead of an explicit `key ID`. Titles moved from `db/schema.cds` to `srv/annotations/Products.cds` and localized via `_i18n/i18n.properties` (en) and `i18n_ru.properties`.
- Added `@assert.range` on `price` and `stock`. Added `_i18n/messages.properties` for error keys.
- Removed the `srv/annotations/Products/` folder (ui, valuehelps, constraints).

### app
- Feature `categories-code-list`, phase 3, steps 9–11: `app/products/annotations/Products.cds`: the manual `@Common.ValueList` with `CollectionPath: 'Products'` is removed (it suppressed the generation from the CodeList, see LESSONS); on the `category` association added `Common.Text: category.name`, `Common.TextArrangement: #TextOnly`, `Common.ValueListWithFixedValues: true` (ADR-0011: the ValueList is generated by the compiler, the presentation is a dropdown). In `UI.HeaderInfo.Description`, `UI.SelectionFields`, `UI.LineItem`, `UI.FieldGroup #GeneralInfo` the `category` references are replaced with `category_code`: the compiler does not rewrite an association path in a DataField to the foreign key (the EDMX ended up with `Path="category"` on a NavigationProperty, see LESSONS).
- New `app/products/annotations/Categories.cds`: `Common.Text: name` + `Common.TextArrangement: #TextOnly` on `CatalogService.Categories.code`, so that dropdown items show names rather than codes; wired in with a `using` line in `app/products/annotations.cds`. No `@UI.*` for `Categories`, `manifest.json` unchanged.
- Mock mode: `localService/metadata.xml` regenerated (`cds compile '*' --to edmx-v4 -s CatalogService -l en`); `mockdata/Products.json` synchronized with the 15 records of `db/data/my.catalog-Products.csv` (`category_code`, real `imageUrl`; the STATE debt "5 records with placeholder images" is closed); added `mockdata/Categories.json` (6 records) and `Categories_texts.json` (6 records, locale `ru`). Verified on `sap-fe-mockserver`: `Products?$expand=category` resolves, the filter by `category_code` works, `test/flpSandbox.html` returns 200. No new keys in `webapp/i18n` (step 12 skipped by decision of `ux-designer`).
- UI annotations moved to `app/products/annotations/Products.cds` with the entry point `app/products/annotations.cds`. The `currency_code` column removed from the LineItem (the currency is visible in the price formatting via `ISOCurrency`).
- `manifest.json`: `_version` 2.0.0, `minUI5Version` 1.136.0, the obsolete `synchronizationMode` removed. Bootstrap parameters in the html files moved to the current spelling (`ui5lint --fix`).
- `Component.js`: `sap.ui.getCore().byId` replaced with `Element.getElementById` (ui5lint autofix, behavior preserved).
- Mock mode: removed `localService/mockserver.js`, `test/initMockServer.js`, `test/mockServer.html`, `ui5-local.yaml`. Added `ui5-mock.yaml` with `sap-fe-mockserver`; `ui5.yaml` is now proxy only. `mockdata/Products.json` converted to the array format.
- `localService/metadata.xml` regenerated from cds 10 with English labels.

### pipeline
- Agent pipeline: 11 subagents (`.claude/agents/`), the shared `project-protocol` skill, skills `feature`, `spec`, `add-entity`, `gen-docs`, `run-app`, `test-all`, `review`, `retro`, `release-check`, `debug-after-upgrade`, `upgrade-cds`, 11 path-based rules (`.claude/rules/`), `templates/`, 9 ADRs.
- Hooks in `.claude/settings.json` with scripts in `scripts/hooks/`: SessionStart (context and environment check), PreToolUse (file protection, registry reminder), PostToolUse (compilation, prettier, eslint, ui5lint, i18n parity, registry stale marker), SubagentStop (block on linter errors), Stop (registry, STATE, CHANGELOG, `npm test`), PreCompact (checkpoint in STATE).
- Release watcher `scripts/watch-releases.mjs` (31 sources), `docs/framework/versions.json`, `docs/framework/UPDATES.md`, weekly workflow `.github/workflows/release-check.yml`.
- CLAUDE.md rewritten as a constitution (84 lines): documentation map, invariants, commands, prohibitions.
- Installed Node 22.23.2 and `@sap/cds-dk` 10.0.7. Claude Code plugins `ui5@claude-plugins-official` and `cap-developer@cap` enabled at the project level (`.claude/settings.json`).
- Added `.mcp.json`: `cds-mcp` 0.0.5, `fiori-mcp` 1.12.2, `chrome-devtools` 1.8.0, versions pinned.
- Added `docs/architecture/*`, `docs/registry/*` (generated by `scripts/gen-registry.mjs`), `scripts/check-docs-fresh.mjs`, `docs/STATE.md`, `docs/LESSONS.md`, `docs/decisions/`, `templates/`, `.claude/rules/`.
- Pipeline plan: `docs/ai-pipeline-plan.md`; the external memory server was removed from the plan.
- `npm run watch` moved from `cds-serve --watch` (fails in cds 10: `this.load is not a function`) to `cds watch` (`ce05c8a`); `npm start` stays `cds-serve` per the CAP documentation. Found by the `test-ui` agent.

### docs
- The `templates/*.cds` templates got unique namespaces `my.catalog.tpl.*`: CAP MCP compiles all `.cds` files of the project and failed on duplicates. The workaround for the `run_manifest_validation` defect of UI5 MCP 0.2.18 is recorded in LESSONS.
- Feature `categories-code-list` closed (step 17, `docs-keeper`): the specification and the user decisions in `docs/features/categories-code-list/CONTEXT.md` and `PLAN.md` (`1e0bd04`); ADR-0010 (`UPPER_SNAKE` code format for own code lists) and ADR-0011 (the ValueList for an association to a CodeList is generated by the compiler, `Common.Text` is mandatory on the key of an own code list, `Common.ValueListWithFixedValues` for lists without a maintenance screen); `VERIFICATION.md` (`ui-verifier`, manual UI and network check) and `SUMMARY.md` close the feature. `PATTERNS.md` (code list, translatable texts, association target check, value help from a code list, user scenario), `templates/annotations-ui.cds`, `STACK.md` and `TESTING.md` updated for ADR-0010/0011.

### test
- Feature `categories-code-list`, step 13 (`test-ui`): UI tests in `app/products/webapp/test/`. Test Starter scaffold (`testsuite.qunit.html`, `testsuite.qunit.js`, `Test.qunit.html`; UI5 from the same CDN as `index.html` and `flpSandbox.html`, so that `sap.fe.test` and the application are on one version), `integration/opaTests.qunit.js` with a single `JourneyRunner` (`pages/JourneyRunner.js`, started via the FLP intent `products-display`), page objects `pages/ProductsList.gen.js` and `ProductsObjectPage.gen.js` generated by `@sap-ux/ui5-test-writer` (the legacy scaffold and the generator's `.gen` journeys were replaced, reasons in LESSONS), custom page object `pages/CategoryDropdown.js` for the dropdown's typeahead table and the filter tokens, expected texts in `integration/data/CategoryTexts.js` (ASCII, `\u` escapes). Journeys: `FilterProductsByCategoryJourney` (list of 6 names, "Kitchen" 3 rows, "Kitchen"+"Sports" 4, tokens with names), `CategoryShownAsNameJourney` (column, header and Object Page field), `RussianLocaleJourney` (`sap-ui-language=ru`: label "Категория", column, list, cells, Object Page), `EditCategoryOnObjectPageJourney` via `opaTest.skip`: `Products` has no draft, the Object Page does not show Edit. Run with `npm run test:ui` in `app/products` (`ui5-test-runner` against `npm run watch`, `http://localhost:4004/products/webapp/test/testsuite.qunit.html`): `fiori run` (:8080) does not serve `/products/webapp`, which is hardcoded in `flpSandbox.html`. `ui5lint` without errors (`prefer-test-starter` respected).
- Feature `categories-code-list`, steps 9–11 (`fiori-app-dev`): the snapshot `test/__snapshots__/metadata.test.js.snap` updated again (`npx vitest run -u test/metadata.test.js`), the second deliberate update, foreseen by step 14 of the plan. Reason: the UI annotations from `app/` entered the contract: on `Products/category_code` appeared `Common.Text Path="category/name"` with `UI.TextArrangement TextOnly` and `Common.ValueListWithFixedValues Bool="true"`; on `Categories/code` `Common.Text Path="name"` with `TextOnly`; the paths in `UI.HeaderInfo`, `SelectionFields`, `LineItem`, `FieldGroup#GeneralInfo` moved from `category` to `category_code`; the old ValueList with `CollectionPath="Products"` disappeared, only the generated one with `CollectionPath="Categories"` remains. Test code unchanged, 15 tests green.
- Feature `categories-code-list`, step 7 (`test-backend`): `test/catalog-service.test.js` extended from 7 to 13 tests. New: "lists the 6 seeded categories", "returns localized category names with English fallback" (`Accept-Language` `ru`, `en` and fallback `de` → English name), "filters products by category code" (replaces "filters by category"), "expands the category of a product", "rejects a product without a category" (`ASSERT_MANDATORY`), "rejects an unknown category code" (`ASSERT_TARGET`), "does not allow creating categories" (405); "creates a product with the mandatory fields…" and "rejects a product without a name" moved to `category_code`. The snapshot `test/__snapshots__/metadata.test.js.snap` deliberately updated with `npx vitest -u`: the OData contract changed, `Products.category` (String 50) replaced with `category_code` (String 20) and the `category` navigation; added the entity sets `Categories`, `Categories_texts`, `Capabilities.*Restrictions` on `Categories`, the generated `Common.ValueList` with `CollectionPath="Categories"` on `category_code`; the old ValueList on `Products` removed. `Common.Text`/`TextArrangement`/`ValueListWithFixedValues` will appear after steps 9–10, the snapshot will be updated again in step 14.
- Added `test/catalog-service.test.js` (7 tests: list, price as a string, filter, creation with managed fields, `@mandatory`, `@assert.range`, currency code list) and `test/metadata.test.js` (EDMX snapshot in `test/__snapshots__/`, HTTP `$metadata` with English labels). `vitest.config.mjs` with `globals: true`.

### deps
- `app/products`: `@sap-ux/ui5-test-writer` 1.9.6 and `ui5-test-runner` 5.14.0 in devDependencies (user decision, feature `categories-code-list`, step 13); `report/` (runner output) added to `.gitignore`. On the first run the runner installs `puppeteer` globally by itself (`npm install puppeteer -g`); on the developer machine `puppeteer` 25.10.0 and Chrome for Testing 152 (`puppeteer browsers install chrome`) are installed.
- Added `@sap/cds-common-content` 3.2.0: ISO data for `sap.common.Currencies`, `Countries`, `Languages`; imported in `db/schema.cds`. Without it the currency value help was empty.
- Backend moved from `@sap/cds` 8 to 10 (`@cap-js/sqlite` 3, `@cap-js/cds-test` 1, Vitest 5, ESLint 10 with `@sap/eslint-plugin-cds`, Prettier 3). `package.json` is now ESM (`"type": "module"`), `engines.node >= 22`.
- UI: `@ui5/cli` 4, `@sap/ux-ui5-tooling` 1.32, `@sap-ux/ui5-middleware-fe-mockserver` 2, `@ui5/linter` 1, `@sap-ux/eslint-plugin-fiori-tools` 10.

### repo
- Removed the foreign `ecosystem.config.js`. `db.sqlite-shm`, `db.sqlite-wal` removed from git; `.gitignore` extended (`*.sqlite-*`, `.claude/settings.local.json`, `.cds-upgrade/`).
- README rewritten for the new structure and commands.
