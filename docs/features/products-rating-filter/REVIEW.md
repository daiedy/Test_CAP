# products-rating-filter: review

Date: 2026-09-25. Agent: `reviewer`. PLAN step 8. Branch `feature/products-rating-filter` vs `main` (commits 5775578, 3d56b96, 1b671d6, b6b5790). All gates below were re-run by the reviewer in this session.

## Blocking

None.

## Important

- `app/products/webapp/ext/fragment/RatingRangeFilter.js` (whole file) is not Prettier-formatted: double quotes and 4-space indentation, while `.prettierrc` and CONVENTIONS ("Formatting: Prettier ... 2 spaces, single quotes") require single quotes and 2 spaces. `npx prettier --list-different webapp/ext webapp/test` in `app/products` lists it; every other file new in this feature passes (the other listed files were already unformatted on `main`, checked with `git show main:`). Neither `ui5lint` nor `lint:js` enforces Prettier, so no gate caught it. Non-blocking (style only, no behaviour change). → `fiori-app-dev`: `npx prettier --write app/products/webapp/ext/fragment/RatingRangeFilter.js`, re-run `npm run lint` and `npm run lint:js` in `app/products`.
- `docs/architecture/PATTERNS.md:67`, row "User scenario", still prescribes the Editing Status filter "followed by `iExecuteSearch()`". With `liveMode: true` the List Report has no Go button and this feature removed every `iExecuteSearch` from the suite (`grep -rn iExecuteSearch app/products/webapp/test` empty), so the row now describes a way the code no longer uses. PLAN step 9 lists the new "Custom filter field" row and the STATE debt row but not this edit. Non-blocking (phase 6 work). → `docs-keeper` in step 9: replace "followed by `iExecuteSearch()`" with "followed by a polling `iCheckRows(<n>)` whose count differs from the previous state (`liveMode`, no Go button)", example `DraftMarkerInListReportJourney.js`.
- PLAN acceptance criterion "a change of Name, Category, Price, Editing Status or Rating reloads the table once per change (one `Products` request per change)" is not met literally and not fully measured. (a) The first filter change after page load sends two `Products` requests for any field (research section 8: two `search` events from `mdc` `FilterBarBase._checkAndNotify`, reproduced with the standard Name field; the slider handler calls `setFilterValues` once; VERIFICATION scenario B). The final rows always match the final conditions; every later change sends one request. The PLAN risk this criterion guards is "a request per keystroke or per drag pixel", which did not happen. My judgement: this is a wording defect of the criterion against measured framework behaviour, not a defect of the feature, and it does not need a separate user decision; it must not be silently ticked. (b) Category, Price and Editing Status request counts were not measured (VERIFICATION "Not verified"); only Rating and Name were. Non-blocking. → `architect` (owner of PLAN wording), applied by `docs-keeper` in step 9: reword to "every filter change reloads the table without Go; the first change after page load sends two `Products` requests (framework `liveMode`, research section 8), every later change one", mark the three unmeasured fields as "not measured" in the criterion and in `SUMMARY.md`, and name the deviation in the phase report to the user. If the user wants the three fields measured, `ui-verifier` runs scenario M for them (no code change expected).

## Minor

- `docs/features/products-rating-filter/VERIFICATION.md:44`: new console warning `Invalid empty segment - /Products//Products/rating` (`ODataMetaModel`), once per page load, only with this feature, no visible or functional effect (label renders in en/ru, filter applies and clears in every scenario). Most likely from the converter's label fallback for a manifest filter field without `label`/`property` (research section 2, `_getMissingLabelForManifestFilterFields`), which ADR-0020 prescribes; adding `property` or `label` would be outside the two-entry exception. Not verified from source. → `docs-keeper`: one LESSONS entry ("Pending upstream" or framework note) so the next verifier does not re-investigate it; `architect` only if it later turns out to have an effect.
- `docs/features/products-rating-filter/research/fiori-mcp-custom-filter.md:5-6` still reads "steps 4 and 4c are **not started**. Decision needed from the user or `architect`" and "Options (for the user / `architect`, not taken)". It is the step 3 record, but a reader of the folder or the future `SUMMARY.md` permalink finds no pointer to the outcome. → `docs-keeper`: one line under "Result": "Resolved 2026-09-25: option 1, ADR-0020 section 'Exception to ADR-0007'; `list_functionality` re-run before the edit, unchanged (CHANGELOG)."
- No automated test covers the one-way display direction (condition set from outside → slider follows): `iSeeSlider(..., [0, 5])` at first load would also pass with an unbound `range` (a RangeSlider defaults to `[min, max]`), and `iSeeRange` after `iSetRange` reads back what the action wrote. The direction is proven only by the `fiori-app-dev` measurement with `StateUtil.applyExternalState` (research section 8) and the connector-fixed variant restore described in VERIFICATION D. Not an acceptance criterion; the variant scenario becomes the natural guard once the sandbox flex-connector issue is fixed. → `test-ui`, optional, together with the flex-connector backlog issue: an OPA step that applies a `rating BT` condition through the filter bar state (not the slider) and asserts `getRange()`.

## Checked and in order

- ADR-0020 followed: `range` bound `mode: 'OneWay'` to `filterValues>` with type `sap/fe/macros/filter/type/Range` and `formatter: handler.formatRange`; handler wired with `core:require`; `change` → `onRangeChange` calling `this.setFilterValues('rating', 'BT', [lo, hi])` or `this.setFilterValues('rating')` at 0..5; named pure `toCondition` with QUnit; no `ext/controller/`, no `controllerExtensions` in the manifest, no private JSON model.
- `git diff main -- app/products/webapp/manifest.json`: exactly `controlConfiguration["@com.sap.vocabularies.UI.v1.SelectionFields"].filterFields.rating` = `{ template: "products.ext.fragment.RatingRangeFilter", availability: "Default", position: { placement: "After", anchor: "price" } }` (no `label`, no `property`) and `liveMode: true` on `ProductsList`; the only other changed line is the comma after `enhanceI18n`. Nothing else.
- `research/fiori-mcp-custom-filter.md` exists with the `list_functionality` output (full id list, only `[ProductsList, filterBar, hideFilterBar]` for the filter bar) and the `get_functionality_details` result.
- `grep -rn iExecuteSearch app/products/webapp/test` → empty (exit 1). The 5 replaced waits keep counts that differ from the previous state (15→3, 3→4, 15→1, 1→15, 15→3); the new journey follows the same rule (15→8→9→15, 3→2→3).
- OData contract unchanged: `git diff --numstat main` has no line for `test/__snapshots__/metadata.test.js.snap` or `app/products/webapp/localService/metadata.xml`; no file under `db/`, `srv/` or `app/*/annotations` changed.
- Tests meaningful: the journey fails on `main` (no `...--rating--RatingRangeSlider`, no `::CustomFilterField::rating` item, Go button visible so `iSeeNoGoButton`'s `visible: false` + no DOM ref fails, `getLiveMode()` false); `iSetRange` fires the real `change` so the real handler and `setFilterValues` run; `iSeeRatingCondition` asserts the mdc FilterBar condition (`BT` and values, or none); `iSeeRowsWithRatingWithin` checks every row context's `rating`; the Adapt Filters check counts items by key and by label, so a custom field under a second key would give two items and fail. QUnit covers bands, full range, reversed, rounding, out-of-range and the `MIN_SAFE_INTEGER` empty-condition value.
- Layers: no annotation or `db/`/`srv/` change; no `console.log`, no global `sap.*`, `sap.ui.define` only; no user-facing string in code (label from `Common.Label`, measured en/ru).
- i18n: no new key needed; `ru` label asserted in `RussianLocaleJourney.js` via `RatingTexts.labels.ru` (`\u` escapes).
- Language: no Cyrillic in any changed file except `VERIFICATION.md` (rendered UI quoted as evidence, allowed); agent memory files English.
- Duplicates: registry had no fragment, handler or formatter (`UI-ARTIFACTS.md` "_none_" on `main`); `RatingRangeSlider.iSeeFilterLabel` is not a duplicate of `CategoryDropdown.iSeeFilterFieldLabel` (that one resolves `::FilterField::<property>`, which does not match `::CustomFilterField::rating`, and does not assert the slider child); `RatingTexts.js` extended, not copied.
- Scenario D (variant restore) not raised: pre-existing sandbox flex-connector gap, reproduced on `main`, separate backlog issue by user decision 2026-09-25.
- STATE `Now` in template shape; CHANGELOG top section has the phase 3 lines for this change; registry fresh; ADR-0020 status in the accepted template form.
- Gates re-run by the reviewer:
  - `npm run lint` → `cds lint`, no findings.
  - `npm test` → `Test Files 7 passed (7)`, `Tests 69 passed (69)`.
  - `node scripts/check-docs-fresh.mjs` → `docs/registry is fresh.`
  - `node scripts/check-feature-docs.mjs products-rating-filter` → PLAN and CONTEXT keep the template shape.
  - `app/products`: `npm run lint` → `UI5 linter report: Success! No findings detected.`; `npm run lint:js` → `0 errors, 1 warning` (known `Component.js` timeout).
  - `npm run test:ui` against `npx cds serve --in-memory --port 4004` (started and stopped by the reviewer): OPA modules 4/4, 3/3, 6/6, 4/4, 3/3, 8/8, 6/6, 2/2 = 36/36, `unit/unitTests` 2/2, 0 skipped.

## Verdict

ready to commit (zero blocking findings; the Prettier fix is a one-command change for `fiori-app-dev`, the rest is phase 6 work for `docs-keeper`)
