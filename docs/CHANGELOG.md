# Changelog

Format: date, area, what changed. Maintained by `docs-keeper`, all agents add to it. New entries on top.

## 2026-09-07

### pipeline
- CI: `.github/workflows/ci.yml` with three jobs (backend lint, tests, registry freshness and compile incl. templates; UI ui5lint and ESLint; OPA5 journeys with `ui5-test-runner` against `cds serve`). Dependabot: `.github/dependabot.yml`, weekly, grouped (sap-cap, sap-ui5-fiori, tooling), cooldown 7/14 days, CAP majors excluded (they go through `/upgrade-cds`), GitHub Actions updates included.
- `app/products/eslint.config.mjs` with `@sap-ux/eslint-plugin-fiori-tools`; scripts `lint:js`, `lint:js:fix`. Baseline: 1 warning (the deliberate `setTimeout` in `Component.js`).

### app
- FLP sandbox works through `npm start` on :8080: `id="sap-ushell-bootstrap"` on the bootstrap tag (livereload was picked as the last script and broke the config URL), relative app URL in the sandbox config, inline scripts moved to `test/flpSandboxConfig.js`, `test/flpSandboxInit.js`, `sandboxConfig.js`, `sandboxInit.js` (CSP warnings gone). `Container.createRenderer` calls carry `ui5lint-disable-next-line` until the New Sandbox migration.


### pipeline
- Retro follow-ups: `maxTurns` raised for test-ui, ui-verifier, docs-keeper (80), reviewer (50), architect and test-backend (60); `feature` skill updates STATE after each phase gate; `architect` decides on draft before planning edit UIs; `reviewer` checks the language rule; `test-all` gained a dev-server smoke step and a Cyrillic scan.
- English-only rule: CLAUDE.md invariant 10, `project-protocol` section 4, PostToolUse hook flags Cyrillic outside i18n bundles, `.texts.csv` and tests.
- CLAUDE.md: request-to-skill routing table (which skill for which request, which agents it runs, who starts it).
- Remaining Russian documents translated to English: STATE, CHANGELOG, LESSONS, feature docs of `categories-code-list`, ADR-0010, ADR-0011, agent memories.

### test
- `test/metadata.test.js`: new test that `app/products/webapp/localService/metadata.xml` equals the compiled EDMX of the whole model (16 tests total).


### db, srv
- Feature `categories-code-list`, phase 2, steps 3–6: `db/schema.cds`: new entity `my.catalog.Categories : sap.common.CodeList` with `key code : String(20)` (ADR-0010). `Products.category` moved from `String(50)` to `Association to Categories`; the foreign key `category_code : String(20)` is generated by the compiler; the OData contract changes from `category` (Edm.String) to `category_code` (Edm.String 20) plus the `category` navigation.
- Data: `db/data/my.catalog-Categories.csv` (6 codes `ACCESSORIES`, `ELECTRONICS`, `FURNITURE`, `KITCHEN`, `SPORTS`, `STATIONERY`, en names, alphabetical order) and `my.catalog-Categories.texts.csv` (6 rows, locale `ru` only); in `my.catalog-Products.csv` the `category` column is renamed to `category_code`, the values are replaced with codes, IDs and the other columns are unchanged.
- `srv/catalog-service.cds`: explicit projection `@readonly entity Categories` (needed for `@assert.target` and titles). `srv/annotations/Products.cds`: `@assert.target` on `category`. New `srv/annotations/Categories.cds`: titles of `code`/`name`/`descr`; keys `Categories.code`, `Categories.name`, `Categories.descr` in `_i18n/i18n.properties` and `i18n_ru.properties`. No handlers. The OData contract change and the `test/__snapshots__` snapshot are recorded by `test-backend` (step 7).
- `Products` uses the `cuid` aspect instead of an explicit `key ID`. Titles moved from `db/schema.cds` to `srv/annotations/Products.cds` and localized via `_i18n/i18n.properties` (en) and `i18n_ru.properties`.
- Added `@assert.range` on `price` and `stock`. Added `_i18n/messages.properties` for error keys.
- Removed the `srv/annotations/Products/` folder (ui, valuehelps, constraints).

### app
- Feature `categories-code-list`, phase 3, steps 9–11: `app/products/annotations/Products.cds`: the manual `@Common.ValueList` with `CollectionPath: 'Products'` is removed (it suppressed the generation from the CodeList, see LESSONS); on the `category` association added `Common.Text: category.name`, `Common.TextArrangement: #TextOnly`, `Common.ValueListWithFixedValues: true` (ADR-0011: the ValueList is generated by the compiler, the presentation is a dropdown). In `UI.HeaderInfo.Description`, `UI.SelectionFields`, `UI.LineItem`, `UI.FieldGroup #GeneralInfo` the `category` references are replaced with `category_code`: the compiler does not rewrite an association path in a DataField to the foreign key (the EDMX ended up with `Path="category"` on a NavigationProperty, see LESSONS).
- New `app/products/annotations/Categories.cds`: `Common.Text: name` + `Common.TextArrangement: #TextOnly` on `CatalogService.Categories.code`, so that dropdown items show names rather than codes; wired in with a `using` line in `app/products/annotations.cds`. No `@UI.*` for `Categories`, `manifest.json` unchanged.
- Mock mode: `localService/metadata.xml` regenerated (`cds compile '*' --to edmx-v4 -s CatalogService -l en`); `mockdata/Products.json` synchronized with the 15 records of `db/data/my.catalog-Products.csv` (`category_code`, real `imageUrl`; the STATE debt "5 records with placeholder images" is closed); added `mockdata/Categories.json` (6 records) and `Categories_texts.json` (6 records, locale `ru`). Verified on `sap-fe-mockserver`: `Products?$expand=category` resolves, the filter by `category_code` works, `test/flpSandbox.html` returns 200. No new keys in `webapp/i18n` (step 12 skipped by decision of `ux-designer`).
- UI annotations moved to `app/products/annotations/Products.cds` with the entry point `app/products/annotations.cds`. The `currency_code` column removed from the LineItem (the currency is visible in the price formatting via `ISOCurrency`).
- `manifest.json`: `_version` 2.0.0, `minUI5Version` 1.136.0, the obsolete `synchronizationMode` removed. Bootstrap parameters in the html files moved to the current spelling (`ui5lint --fix`).
- `Component.js`: `sap.ui.getCore().byId` replaced with `Element.getElementById` (ui5lint autofix, behavior preserved).
- Mock mode: removed `localService/mockserver.js`, `test/initMockServer.js`, `test/mockServer.html`, `ui5-local.yaml`. Added `ui5-mock.yaml` with `sap-fe-mockserver`; `ui5.yaml` is now proxy only. `mockdata/Products.json` converted to the array format.
- `localService/metadata.xml` regenerated from cds 10 with English labels.

### pipeline
- Agent pipeline: 11 subagents (`.claude/agents/`), the shared `project-protocol` skill, skills `feature`, `spec`, `add-entity`, `gen-docs`, `run-app`, `test-all`, `review`, `retro`, `release-check`, `debug-after-upgrade`, `upgrade-cds`, 11 path-based rules (`.claude/rules/`), `templates/`, 9 ADRs.
- Hooks in `.claude/settings.json` with scripts in `scripts/hooks/`: SessionStart (context and environment check), PreToolUse (file protection, registry reminder), PostToolUse (compilation, prettier, eslint, ui5lint, i18n parity, registry stale marker), SubagentStop (block on linter errors), Stop (registry, STATE, CHANGELOG, `npm test`), PreCompact (checkpoint in STATE).
- Release watcher `scripts/watch-releases.mjs` (31 sources), `docs/framework/versions.json`, `docs/framework/UPDATES.md`, weekly workflow `.github/workflows/release-check.yml`.
- CLAUDE.md rewritten as a constitution (84 lines): documentation map, invariants, commands, prohibitions.
- Installed Node 22.23.2 and `@sap/cds-dk` 10.0.7. Claude Code plugins `ui5@claude-plugins-official` and `cap-developer@cap` enabled at the project level (`.claude/settings.json`).
- Added `.mcp.json`: `cds-mcp` 0.0.5, `fiori-mcp` 1.12.2, `chrome-devtools` 1.8.0, versions pinned.
- Added `docs/architecture/*`, `docs/registry/*` (generated by `scripts/gen-registry.mjs`), `scripts/check-docs-fresh.mjs`, `docs/STATE.md`, `docs/LESSONS.md`, `docs/decisions/`, `templates/`, `.claude/rules/`.
- Pipeline plan: `docs/ai-pipeline-plan.md`; the external memory server was removed from the plan.
- `npm run watch` moved from `cds-serve --watch` (fails in cds 10: `this.load is not a function`) to `cds watch` (`ce05c8a`); `npm start` stays `cds-serve` per the CAP documentation. Found by the `test-ui` agent.

### docs
- The `templates/*.cds` templates got unique namespaces `my.catalog.tpl.*`: CAP MCP compiles all `.cds` files of the project and failed on duplicates. The workaround for the `run_manifest_validation` defect of UI5 MCP 0.2.18 is recorded in LESSONS.
- Feature `categories-code-list` closed (step 17, `docs-keeper`): the specification and the user decisions in `docs/features/categories-code-list/CONTEXT.md` and `PLAN.md` (`1e0bd04`); ADR-0010 (`UPPER_SNAKE` code format for own code lists) and ADR-0011 (the ValueList for an association to a CodeList is generated by the compiler, `Common.Text` is mandatory on the key of an own code list, `Common.ValueListWithFixedValues` for lists without a maintenance screen); `VERIFICATION.md` (`ui-verifier`, manual UI and network check) and `SUMMARY.md` close the feature. `PATTERNS.md` (code list, translatable texts, association target check, value help from a code list, user scenario), `templates/annotations-ui.cds`, `STACK.md` and `TESTING.md` updated for ADR-0010/0011.

### test
- Feature `categories-code-list`, step 13 (`test-ui`): UI tests in `app/products/webapp/test/`. Test Starter scaffold (`testsuite.qunit.html`, `testsuite.qunit.js`, `Test.qunit.html`; UI5 from the same CDN as `index.html` and `flpSandbox.html`, so that `sap.fe.test` and the application are on one version), `integration/opaTests.qunit.js` with a single `JourneyRunner` (`pages/JourneyRunner.js`, started via the FLP intent `products-display`), page objects `pages/ProductsList.gen.js` and `ProductsObjectPage.gen.js` generated by `@sap-ux/ui5-test-writer` (the legacy scaffold and the generator's `.gen` journeys were replaced, reasons in LESSONS), custom page object `pages/CategoryDropdown.js` for the dropdown's typeahead table and the filter tokens, expected texts in `integration/data/CategoryTexts.js` (ASCII, `\u` escapes). Journeys: `FilterProductsByCategoryJourney` (list of 6 names, "Kitchen" 3 rows, "Kitchen"+"Sports" 4, tokens with names), `CategoryShownAsNameJourney` (column, header and Object Page field), `RussianLocaleJourney` (`sap-ui-language=ru`: label "Категория", column, list, cells, Object Page), `EditCategoryOnObjectPageJourney` via `opaTest.skip`: `Products` has no draft, the Object Page does not show Edit. Run with `npm run test:ui` in `app/products` (`ui5-test-runner` against `npm run watch`, `http://localhost:4004/products/webapp/test/testsuite.qunit.html`): `fiori run` (:8080) does not serve `/products/webapp`, which is hardcoded in `flpSandbox.html`. `ui5lint` without errors (`prefer-test-starter` respected).
- Feature `categories-code-list`, steps 9–11 (`fiori-app-dev`): the snapshot `test/__snapshots__/metadata.test.js.snap` updated again (`npx vitest run -u test/metadata.test.js`), the second deliberate update, foreseen by step 14 of the plan. Reason: the UI annotations from `app/` entered the contract: on `Products/category_code` appeared `Common.Text Path="category/name"` with `UI.TextArrangement TextOnly` and `Common.ValueListWithFixedValues Bool="true"`; on `Categories/code` `Common.Text Path="name"` with `TextOnly`; the paths in `UI.HeaderInfo`, `SelectionFields`, `LineItem`, `FieldGroup#GeneralInfo` moved from `category` to `category_code`; the old ValueList with `CollectionPath="Products"` disappeared, only the generated one with `CollectionPath="Categories"` remains. Test code unchanged, 15 tests green.
- Feature `categories-code-list`, step 7 (`test-backend`): `test/catalog-service.test.js` extended from 7 to 13 tests. New: "lists the 6 seeded categories", "returns localized category names with English fallback" (`Accept-Language` `ru`, `en` and fallback `de` → English name), "filters products by category code" (replaces "filters by category"), "expands the category of a product", "rejects a product without a category" (`ASSERT_MANDATORY`), "rejects an unknown category code" (`ASSERT_TARGET`), "does not allow creating categories" (405); "creates a product with the mandatory fields…" and "rejects a product without a name" moved to `category_code`. The snapshot `test/__snapshots__/metadata.test.js.snap` deliberately updated with `npx vitest -u`: the OData contract changed, `Products.category` (String 50) replaced with `category_code` (String 20) and the `category` navigation; added the entity sets `Categories`, `Categories_texts`, `Capabilities.*Restrictions` on `Categories`, the generated `Common.ValueList` with `CollectionPath="Categories"` on `category_code`; the old ValueList on `Products` removed. `Common.Text`/`TextArrangement`/`ValueListWithFixedValues` will appear after steps 9–10, the snapshot will be updated again in step 14.
- Added `test/catalog-service.test.js` (7 tests: list, price as a string, filter, creation with managed fields, `@mandatory`, `@assert.range`, currency code list) and `test/metadata.test.js` (EDMX snapshot in `test/__snapshots__/`, HTTP `$metadata` with English labels). `vitest.config.mjs` with `globals: true`.

### deps
- `app/products`: `@sap-ux/ui5-test-writer` 1.9.6 and `ui5-test-runner` 5.14.0 in devDependencies (user decision, feature `categories-code-list`, step 13); `report/` (runner output) added to `.gitignore`. On the first run the runner installs `puppeteer` globally by itself (`npm install puppeteer -g`); on the developer machine `puppeteer` 25.10.0 and Chrome for Testing 152 (`puppeteer browsers install chrome`) are installed.
- Added `@sap/cds-common-content` 3.2.0: ISO data for `sap.common.Currencies`, `Countries`, `Languages`; imported in `db/schema.cds`. Without it the currency value help was empty.
- Backend moved from `@sap/cds` 8 to 10 (`@cap-js/sqlite` 3, `@cap-js/cds-test` 1, Vitest 5, ESLint 10 with `@sap/eslint-plugin-cds`, Prettier 3). `package.json` is now ESM (`"type": "module"`), `engines.node >= 22`.
- UI: `@ui5/cli` 4, `@sap/ux-ui5-tooling` 1.32, `@sap-ux/ui5-middleware-fe-mockserver` 2, `@ui5/linter` 1, `@sap-ux/eslint-plugin-fiori-tools` 10.

### repo
- Removed the foreign `ecosystem.config.js`. `db.sqlite-shm`, `db.sqlite-wal` removed from git; `.gitignore` extended (`*.sqlite-*`, `.claude/settings.local.json`, `.cds-upgrade/`).
- README rewritten for the new structure and commands.
