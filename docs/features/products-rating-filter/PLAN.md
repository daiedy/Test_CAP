# products-rating-filter: plan

Date: 2026-09-25. Status: approved (user, 2026-09-25). Gate mode: autonomous.

Shape (ADR-0018): one criterion per line under "Acceptance criteria", table rows only under "Steps"; explanations belong in `CONTEXT.md` or `research/`. `node scripts/check-feature-docs.mjs <name>` verifies the shape at the phase 1 gate.

Contract rule: a step that changes the OData model (`db/*.cds`, `srv/**/*.cds`, `app/*/annotations/*.cds`) schedules `npx vitest -u` and `cds compile '*' --to edmx-v4 -s CatalogService -l en > app/products/webapp/localService/metadata.xml` in the same phase, never in a later one; the sync test in `test/metadata.test.js` turns red at that phase's gate otherwise (PATTERNS "OData contract").

This plan follows the user's answers of 2026-09-25 (CONTEXT "User decisions"): RangeSlider (ADR-0020), 0..5 step 1, slider only, `liveMode: true` together with this feature. **The OData contract is untouched in every phase**: no step edits `db/`, `srv/` or `app/*/annotations`, so neither `npx vitest -u` nor the `metadata.xml` regeneration is scheduled; the gate figure for `test/__snapshots__/metadata.test.js.snap` and `app/products/webapp/localService/metadata.xml` is "no line in `git diff --numstat main`" at phases 2 and 3 (`liveMode` is a `manifest.json` page setting, not part of `$metadata`).

## Acceptance criteria
- [x] The List Report filter bar shows a Rating filter after Price as a `sap.m.RangeSlider` from 0 to 5 with step 1, both handles at 0 and 5 on first load, verified by OPA5 "The rating filter is a slider from 0 to 5 labelled Rating" (`RatingRangeFilterJourney.js`).
- [x] On first load the filter bar holds no condition for `rating` (`sap.ui.mdc.FilterBar#getConditions().rating` empty) and the table shows 15 rows, verified by the same OPA5 test.
- [x] Setting the range to 4..5 shows 8 rows, all with rating 4 or 5, with no Go press (`liveMode`), verified by OPA5 "Rating 4 to 5 shows 8 products".
- [x] Setting the range to 2..4 shows 9 rows with no Go press, verified by OPA5 "Rating 2 to 4 shows 9 products".
- [x] The rating range combines with the category filter (KITCHEN and 5..5 give 2 rows, no Go press), verified by OPA5 "The rating range combines with the category filter".
- [x] Moving the slider back to 0..5 removes the `rating` condition and shows 15 rows again with no Go press, verified by OPA5 "The full range removes the rating filter".
- [x] The filter label is the `Products.rating` text in `en` and `ru` (`RatingTexts.labels`), verified by OPA5 "The rating filter is a slider from 0 to 5 labelled Rating" and the `ru` assertion added to `RussianLocaleJourney.js`.
- [x] Adapt Filters lists Rating once (the slider), with no second standard `rating` condition field, verified by OPA5 "Adapt Filters offers the rating slider only".
- [x] The mapping from a slider range to a condition returns `BT [lo, hi]` for a band and "clear" for 0..5, verified by QUnit "toCondition maps a band to BT and the full range to clear" (`webapp/test/unit/RatingRangeFilter.qunit.js`).
- [x] The display formatter turns an empty or out-of-range value into a range inside 0..5, verified by QUnit "formatRange keeps the slider inside 0..5".
- [x] The List Report filter bar has no Go button (`liveMode: true` on `ProductsList`), verified by OPA5 "The List Report applies filters without a Go button" (mdc FilterBar `getLiveMode()` true, no visible search button; ids measured).
- [x] The 5 `iExecuteSearch()` calls in `FilterProductsByCategoryJourney.js` (2), `DraftMarkerInListReportJourney.js` (2) and `RussianLocaleJourney.js` (1) are replaced by a wait for the table, no `iExecuteSearch` is left under `webapp/test/`, and `npm run test:ui` reports all 28 previous `opaTest`s plus the new ones passed, 0 skipped.
- [x] `git diff main -- app/products/webapp/manifest.json` contains only the two ADR-0020 exception entries on `ProductsList` (`filterFields.rating` with `template`, `availability`, `position` and no `label` or `property`; `liveMode: true`), edited by hand in the user-started `PIPELINE_ALLOW_PROTECTED=1` session, and `research/fiori-mcp-custom-filter.md` records the `list_functionality` output that shows no MCP functionality for either, verified by the reviewer in `REVIEW.md`.
- [x] OData contract unchanged: `git diff --numstat main` has no line for `test/__snapshots__/metadata.test.js.snap` or `app/products/webapp/localService/metadata.xml`, and `npm test` is green (69 tests).
- [x] `ui5lint` reports 0 problems and `npm run lint:js` 0 errors in `app/products` (the known `Component.js` timeout warning excepted).
- [x] Measured on `npx cds serve --in-memory --port 4004`: the first `Products` request carries no `rating` in `$filter`, setting 4..5 sends `rating ge 4 and rating le 5` on the handle's release without a Go press, and the console shows no RangeSlider "not in the range" warning, recorded in `research/custom-filter-mechanism.md` section 8 and in `VERIFICATION.md`.
- [x] `ui-verifier` confirms in a browser: mouse drag and keyboard (arrow keys on a handle) change the range and the table reloads without Go, `viewer` gets the same filter, `ru` label, mock mode filters too; a saved variant restoring the range and rows is **not verifiable in this sandbox** (missing flexibility connector, reproduced on `main`, separate backlog issue by user decision 2026-09-25), recorded in `VERIFICATION.md` scenario D.
- [x] `ui-verifier` confirms in a browser (en and ru, `alice` and `viewer`): no Go button in the List Report filter bar; with `liveMode` the first filter change after page load sends two `Products` requests (framework, mdc `FilterBarBase._checkAndNotify`; measured for Rating and reproduced for the standard Name field), every later change sends one, and the final rows always match the final conditions; Category, Price and Editing Status request counts are **not measured**; recorded in `VERIFICATION.md`.
- [x] ADR-0020 accepted and `PATTERNS.md` has the row "Custom filter field" with this feature as the example; `CONVENTIONS.md` names `ext/fragment/<Name>.js`.
- [x] Documentation updated: registry (`UI-ARTIFACTS.md` lists the fragment and handler), STATE, CHANGELOG, SUMMARY.

## Steps

| # | Phase | Agent | Files | Pattern | Check |
|---|---|---|---|---|---|
| 1 | 1 Spec | `architect`, `ux-designer` (done) | `docs/features/products-rating-filter/CONTEXT.md`, `PLAN.md`, `research/custom-filter-mechanism.md`, `SCREENS.md` (ux-designer); `docs/decisions/ADR-0020-custom-filter-field.md` (proposed) | ADR template; feature templates | `node scripts/check-feature-docs.mjs products-rating-filter` |
| 2 | 1 Spec gate | `architect` (done 2026-09-25), then `ux-designer` for the `SCREENS.md` `liveMode` edits | `docs/decisions/ADR-0020-custom-filter-field.md`: replace the whole `Status:` sentence with the accepted form of `templates/adr.md`; `CONTEXT.md` "User decisions" filled with the four answers; `SCREENS.md` rewritten for `liveMode` (no Go button) by `ux-designer` | ADR template; feature templates | `grep '^Date:' docs/decisions/ADR-0020-custom-filter-field.md` shows the accepted form only; `node scripts/check-feature-docs.mjs products-rating-filter` passes; `SCREENS.md` names no Go press |
| 3 | 2 UI | `fiori-app-dev` (done 2026-09-25: no functionality for `filterFields` or `liveMode` in `fiori-mcp` 1.12.2; user decision: ADR-0020 exception) | `docs/features/products-rating-filter/research/fiori-mcp-custom-filter.md` (new): the output of `list_functionality` for `app/products` and of `get_functionality_details` for the custom filter functionality (id, parameters, files it generates); no file under `app/` changed yet | Manifest change (ADR-0007) | the file names the functionality id and each parameter; `git status app/` clean. If no functionality covers a custom filter field: stop and report, do not edit `manifest.json` |
| 4 | 2 UI | `fiori-app-dev` in a session the user starts with `PIPELINE_ALLOW_PROTECTED=1` | `app/products/webapp/manifest.json` by hand under the ADR-0020 exception to ADR-0007, this one entry only (`ProductsList` → `controlConfiguration` → `@com.sap.vocabularies.UI.v1.SelectionFields` → `filterFields.rating`: `template: products.ext.fragment.RatingRangeFilter`, `availability: Default`, `position: { placement: After, anchor: price }`, no `label`, no `property`); `app/products/webapp/ext/fragment/RatingRangeFilter.fragment.xml` (RangeSlider `min 0`, `max 5`, `step 1`, `range` one-way from `filterValues>` with type `sap.fe.macros.filter.type.Range` and `formatter: handler.formatRange`, `change: handler.onRangeChange`); `app/products/webapp/ext/fragment/RatingRangeFilter.js` (named `toCondition`, `formatRange`, `onRangeChange` calling `this.setFilterValues('rating', 'BT', [lo, hi])` or `this.setFilterValues('rating')`, JSDoc) | ADR-0020 (row "Custom filter field" and section "Exception to ADR-0007"); Custom section or column; research `custom-filter-mechanism.md` sections 2-4 | `mcp__ui5-mcp-server__run_manifest_validation` (on the known draft-06 schema error `ui5lint` alone is the gate); `git diff main -- app/products/webapp/manifest.json` shows only `controlConfiguration` → `@com.sap.vocabularies.UI.v1.SelectionFields` → `filterFields.rating`; `npm run lint` and `npm run lint:js` in `app/products`; `npm test` green; `git diff --numstat main` has no line for the two contract files; on `npx cds serve --in-memory --port 4004` measure and record in `research/custom-filter-mechanism.md` section 8: first `$batch` `Products` GET without `rating`, slider 4..5 (after step 4c) sends `rating ge 4 and rating le 5`, label en/ru, console free of RangeSlider warnings |
| 4c | 2 UI | `fiori-app-dev` in the same user-started `PIPELINE_ALLOW_PROTECTED=1` session | `app/products/webapp/manifest.json` `ProductsList` → `options` → `settings` → `liveMode: true` by hand under the ADR-0020 exception to ADR-0007, this one entry only | ADR-0020 section "Exception to ADR-0007" | `mcp__ui5-mcp-server__run_manifest_validation` (on the known draft-06 schema error `ui5lint` alone is the gate); `npm run lint` in `app/products`; `git diff main -- app/products/webapp/manifest.json` shows the two exception entries and nothing else; on `npx cds serve --in-memory --port 4004` the filter bar shows no Go button and each filter change sends one `Products` request, recorded in `research/custom-filter-mechanism.md` section 8; the existing journeys are expected red until step 6a |
| 5 | 3 UI tests | `test-ui` | `app/products/webapp/test/unit/unitTests.qunit.js` (new), `app/products/webapp/test/unit/RatingRangeFilter.qunit.js` (new: `toCondition`, `formatRange`), `app/products/webapp/test/testsuite.qunit.js` (entry `unit/unitTests`) | Formatter or extension; skill `ui5-best-practices-qunit` | `npm run test:ui` against `npx cds serve --in-memory --port 4004`: the QUnit page passes |
| 6 | 3 UI tests | `test-ui` | `app/products/webapp/test/integration/RatingRangeFilterJourney.js` (new, 8 `opaTest`s incl. Adapt Filters, "The List Report applies filters without a Go button" and teardown; no `iExecuteSearch`, every change is followed by a row assertion that polls), `pages/RatingRangeSlider.js` (new `OpaBuilder` page object in the shape of `CategoryDropdown.js`: set the range through the control API and fire `change`, assert `getRange()`, the `rating` condition of the mdc FilterBar and the label; ids measured on the running app), `pages/JourneyRunner.js` (register the page), `data/RatingTexts.js` (ranges and counts of research section 6), `opaTests.qunit.js` (journey after `RatingShownAsStarsJourney`, before `DraftMarkerInListReportJourney`), `RussianLocaleJourney.js` (one `ru` label assertion) | User scenario; skill `ui5-best-practices-opa5` | `npm run test:ui` (after step 6a): 28 previous plus the new `opaTest`s passed, 0 skipped; `npm run lint:js` 0 errors |
| 6a | 3 UI tests | `test-ui` | `FilterProductsByCategoryJourney.js` (2), `DraftMarkerInListReportJourney.js` (2), `RussianLocaleJourney.js` (1): each `When.onTheProductsList.onFilterBar().iExecuteSearch()` is removed and the next `Then.onTheProductsList.onTable().iCheckRows(<n>)` becomes the wait (OPA polls until the reloaded rows match; each expected count differs from the count before the change, so a stale table cannot pass); scenarios, names and counts otherwise unchanged | User scenario; skill `ui5-best-practices-opa5`; research section 5 | `grep -rn iExecuteSearch app/products/webapp/test` empty; `npm run test:ui` against `npx cds serve --in-memory --port 4004`: all 28 previous `opaTest`s plus the new ones passed, 0 skipped; `npm run lint:js` 0 errors |
| 7 | 4 Verification | `ui-verifier` | `docs/features/products-rating-filter/VERIFICATION.md`; reads `research/custom-filter-mechanism.md` sections 3 and 8 | | scenarios of `SCREENS.md` plus: first load without `rating` condition (`$batch` evidence), no Go button and a reload on each filter change (Name, Category, Price, Editing Status, Rating; one `Products` request per change), mouse and keyboard, 4..5 → 8 rows without Go, variant save and restore, `viewer`, `ru`, mock mode (`npm run start-mock`); console: no message that is new against the console section of the latest `VERIFICATION.md` on `main` (known noise: the `Permissions/isEditor` `$select` TypeError of STATE open debt) |
| 8 | 5 Review | `reviewer` | `docs/features/products-rating-filter/REVIEW.md` | | zero blocking findings; ADR-0020 followed (one-way binding, `setFilterValues`, no controller extension); `git diff main -- app/products/webapp/manifest.json` holds exactly the two ADR-0020 exception entries (`filterFields.rating` with `template`, `availability: Default`, `position { After, price }`, no `label`, no `property`; `liveMode: true`) and nothing else, and `research/fiori-mcp-custom-filter.md` exists with the `list_functionality` output; no `iExecuteSearch` left in the OPA5 suite |
| 9 | 6 Documentation | `docs-keeper` | `docs/registry/*` (`npm run docs:registry`), `docs/STATE.md` (remove the open-debt row "List Report needs the Go button", closed by `liveMode: true` in this feature), `docs/CHANGELOG.md`, `docs/architecture/PATTERNS.md` (row "Custom filter field" with the example `app/products/webapp/ext/fragment/RatingRangeFilter.*`), `docs/architecture/CONVENTIONS.md` (`ext/fragment/<Name>.js`), `docs/features/products-rating-filter/SUMMARY.md`, `docs/LESSONS.md` if a measurement contradicts the research, and in any case a LESSONS entry (section "Pending upstream") plus a STATE open-debt row: `fiori-mcp` 1.12.2 `list_functionality` exposes no `filterFields` and no `liveMode`, both set by hand under the ADR-0020 exception; `upstream-watcher` re-checks on each `fiori-mcp` bump | ADR-0020 | `node scripts/check-docs-fresh.mjs`; `npm test` (doc-shape tests) green |

## Decisions that require an ADR
- Custom filter field in the FE V4 filter bar (no PATTERNS row): `docs/decisions/ADR-0020-custom-filter-field.md`, accepted by the user 2026-09-25; amended the same day with the section "Exception to ADR-0007" (user decision after step 3: `filterFields.rating` and `liveMode: true` by hand, because `fiori-mcp` 1.12.2 exposes neither).

## Risks
- Materialized at step 3: `fiori-mcp` 1.12.2 has no functionality for `filterFields` or `liveMode`, so both entries are hand edits under the ADR-0020 exception (user decision 2026-09-25); a hand edit can slip in another key or a typo in the template name; detection: `run_manifest_validation` or `ui5lint`, the `git diff main` check of steps 4 and 4c and the reviewer's diff check in step 8, and the OPA5 slider tests (a wrong `template` renders no slider). Scope creep beyond the two entries is a blocking review finding.
- The one-way display binding with `Range` type plus `formatter` is predicted from source, not measured; detection: step 4 measurement (slider shows the condition after a variant switch and after a filter change) and `ui-verifier` variant scenario. If the display does not follow the condition, the formatter reads the conditions instead; that is a change of ADR-0020 before acceptance, not a silent variant.
- The label fallback to `Common.Label` is read from the converter source; detection: step 4 (en/ru on :4004) and the OPA5 label assertions. If it fails, add `ProductsList.ratingFilter.label` in `webapp/i18n/i18n.properties`, `i18n_en.properties` and `i18n_ru.properties` in step 4; a `label` key is outside the two-entry ADR-0020 exception and has no MCP functionality in 1.12.2, so `fiori-app-dev` stops and the user decides before it is set.
- `sap.fe.test` has no API for a custom filter control, and FE ids of the wrapper are unknown ahead of rendering; detection: `test-ui` probes ids with a failing assert (its memory), never guesses.
- A programmatic `setRange` + `fireChange` in OPA5 bypasses pointer handling; covered by `ui-verifier` (mouse drag and arrow keys).
- A leftover draft or a saved variant changes row counts; the journey saves no variant, runs before `DraftMarkerInListReportJourney`, and restores the full range before teardown.
- `docs/registry/UI-ARTIFACTS.md` lists the `ext/` files but not the manifest wiring; the reviewer checks the manifest by diff.
- Products with `rating` null (possible, `rating` is not mandatory) must not vanish without a rating filter; covered by `toCondition` clearing on 0..5 (QUnit) and the first-load criterion.
- `liveMode` reloads on every filter change: a drag must fire `change` only on the handle's release (`RangeSlider` `change` vs `liveChange`, predicted) and the Name field only on Enter or leave; detection: step 4c network log (one `Products` request per change) and `ui-verifier`. A request per keystroke or per drag pixel is a defect of step 4, reported, not tuned silently.
- Without `iExecuteSearch` an OPA5 row assertion could pass on the stale table if the expected count equals the count before the change; step 6a keeps only waits whose count differs from the previous state (all 5 do today), and the new journey follows the same rule.
- Between step 4c and step 6a the 3 existing journeys are red by design (no Go button to press); both steps belong to the orchestrator's UI phase (`fiori-app-dev`, then `test-ui`), whose single commit is made only after `npm run test:ui` is green with step 6a done.
- `FE_FilterSearch` (Ctrl+Enter) is not registered with `liveMode` (research section 5); `SCREENS.md` keyboard text is updated by `ux-designer` accordingly.

## Open questions
None: Q1-Q4 answered by the user on 2026-09-25, and the manifest exception after step 3 decided the same day (CONTEXT "User decisions").
