# Project state

Updated by the `docs-keeper` agent at the end of every task and by the PreCompact hook. The first 40 lines are printed at the start of every session.

## Where we are

- Date: 2026-09-23
- 2026-09-23: retro of `catalog-authorization` (`/retro`, on the user's request, branch `chore/retro-catalog-authorization`). Three pending LESSONS entries and six pipeline findings transferred, see `docs/CHANGELOG.md` 2026-09-23 `pipeline`. The registry gate deadlock that blocked four times during the feature is closed by **ADR-0017**: the git-based gates audit `docs/registry/**` through `protectedWriteHit()`, which lets the generator's output through and still blocks a header-less hand write; `docs/registry/.stale` is a marker, exempt and gitignored; `subagent-stop.mjs` no longer tells a subagent to `git checkout` a file the Bash guard forbids it to touch. New `test/hooks-registry-gate.test.js` (7 tests) pins the table and the new `checkMockdata` PostToolUse check; backend suite **45 tests**. Agents now create their document deliverable on disk before researching (protocol section 2.10; `reviewer` writes `REVIEW.md` as it goes), the orchestrator verifies but does not fix (`feature` skill), `test-ui` verifies each artifact as it is created, `architect` labels scratchpad figures with their phase, `ui-verifier` checks visibility per page. MCP audit of the run: 112 `mcp` and 42 `edit` records over nine agent types, zero `justification` / `skipped-unjustified`, so `MCP_RULES` is unchanged.
- Feature `catalog-authorization` **complete, all six phases**, merged into `main` on 2026-09-23 (`284f492`, `--no-ff`, pushed; CI run 35870986435 green). Phase 1 at `3fdd195` (PLAN approved, ADR-0013 accepted, ux-designer Screens with seven UX risks). Phase 2 at `df96757` (steps 4-7): `@requires` on the service, `@restrict` READ->`CatalogViewer` / `*`->`CatalogEditor` on `Products`, read-only singleton `CatalogService.Permissions` (`isEditor`), the project's **first handler** `srv/catalog-service.js` (UI signal only, enforcement stays declarative), mock users in `package.json`. Phase 3 at `4e443e1`/`d3a4db0` (steps 9-11): three `UI.*Hidden` annotations with `$edmJson` in `app/products/annotations/Products.cds` (presentation layer, ADR-0004), each term emitted twice in the EDMX; `app/products/ui5-test-runner.json` for the OPA Basic auth; mock fixture `mockdata/Permissions.json`; new journey `RoleAwareActionsJourney` registered last as the guard for risk R6. Contract now **+50 EDMX lines, 0 removed** against `main`, `metadata.xml` byte-identical to a fresh compile. Gate closed with `npm test` **38 passed, 0 skipped**, `cds lint` clean, `ui5lint` "No findings detected", `npm run test:ui` **25 opaTests, 0 skipped** against `cds serve --in-memory`. Three defects were found and fixed in flight, all by verification rather than by the authoring agent: the architect's `+50` was the feature end state, not phase 2 (+20 there); the planned one-line `<Not><Path>...</Path></Not>` assertion never matches because the compiler pretty-prints `$edmJson`, so the test normalizes whitespace between tags; and the mock fixture shape, which was settled the wrong way first and corrected in phase 3 (`d3a4db0`) - the object form was committed on a plain `curl GET`, a path the application never uses; FE issues `GET Permissions?$select=isEditor` **inside `$batch`**, and only the array form works on that path. **Phase 4** (`ui-verifier`, `VERIFICATION.md`, 11 screenshots): the blocking viewer criterion PASSED - 15 rows, no Create, no Delete, no Selection column at all, no Edit/Delete on the Object Page, `Permissions` returning `isEditor: false` in the same isolated session. Editor, `ru`, anonymous and the :8080 proxy all pass. **Phase 5** (`reviewer`, `94b5883`): **zero blocking findings**. Root-caused the verifier's console observation to an unguarded UI5 1.152.0 defect (`_Helper.aggregateExpandSelect` throws instead of guarding when a container-qualified `$edmJson` `$Path` is used as an OData V4 data-binding path, `_Helper-dbg.js:238`): deterministic, identical for both roles, and the hide/show decision itself was correct in every one of five independent measurements, so it is accepted as a known, non-blocking console finding rather than a defect - see "Open debt". **Phase 6** (`docs-keeper`): registry, CHANGELOG, PATTERNS, CONVENTIONS, TESTING, ARCHITECTURE, README, `templates/service.test.js`, `SUMMARY.md`, ADR-0013 consequences and LESSONS updated; `PLAN.md` fully ticked, with the console criterion recorded as "not met, accepted with rationale" per the review. Not yet merged to `main`.
- Previous feature: `products-draft-marker` on branch `feature/products-draft-marker`, all six phases complete. Phase 1 committed at `1da73ab` (CONTEXT, PLAN approved by the user, ADR-0015 accepted). Phase 3 committed at `1287c3c` (plan steps 3–5): `Common.SemanticKey: [ name ]` in `app/products/annotations/Products.cds`, `localService/metadata.xml` and the EDMX snapshot regenerated, one new backend contract test, new OPA5 journey `DraftMarkerInListReportJourney` registered last. Gate of phase 3 closed with `cds lint` clean, `ui5lint` "No findings detected", `npm test` 23/23, `npm run test:ui` 23/23 with 0 skipped and 15 products / 0 drafts left behind; the row marker is proven in the OPA log by `state='{isDraft: false}' → true → true (Own Draft filter) → false`. Phase 4 (`ui-verifier`, `docs/features/products-draft-marker/VERIFICATION.md`): all seven mandatory browser scenarios passed, verdict "ready for review", data hygiene proven, the agent's server stopped. Phase 5 (`reviewer`, three independent lens runs): zero confirmed blocking findings. Phase 6 (`docs-keeper`): registry regenerated, CHANGELOG/STATE/PATTERNS/SUMMARY/LESSONS updated, ADR-0015 consequences ticked, PLAN fully ticked. **Merged into `main` on 2026-09-10** (`bdb6321`, `--no-ff`) and pushed; CI run 34394169431 green (backend, UI lint, OPA5 journeys). Retro done the same day, see below.
- Two predictions in `products-draft-marker` `CONTEXT.md`/`PLAN.md` were measured wrong in the browser and were corrected by `docs-keeper` at their source (`CONTEXT.md`, `PLAN.md`, ADR-0015 "Consequences", `docs/LESSONS.md`), not carried forward as fact: (a) the editing-status marker IS a tab stop with a working press handler and a "Last changed on …" popover for the `Draft` type too — the plan's "no tab stop" claim described the neighbouring `ObjectIdentifier` title (`titleActive: false`), not the marker; (b) inside this FE V4 responsive table cell the lock marker does NOT fall back to icon-only below a 600 px window width (measured at 500 px in the `Phone` range, after both a live resize and a full reload: classes stayed `sapMObjectMarkerIcon sapMObjectMarkerText`) — the responsive table pops the other columns first, so the pinned standalone-control API doc does not describe the in-cell behaviour. Two further criteria also measured differently and were corrected the same way: (c) the marker's accessible name is not strictly equal to its own text (FE's own `aria-labelledby` also references the doubled column-header label); (d) the WCAG 2.5.8 target-size measurement (A3) passed the 24×24 CSS px minimum for the tested label (110.97×25 px) — the gap is conditional on label length, not a certainty. None of the four is a defect and none is reachable from the annotation.
- The OPA flake of 2026-09-09 is now bounded, and a bigger finding came out of measuring it. `npm run test:ui` must run against `npx cds serve --in-memory --port 4004`, the way CI starts the server, **not** against `cds watch`: measured 2026-09-10, a long-running `cds watch` made the runner register no test page at all (`testPageUrls: []`, "No test page found (or all filtered out)") and a second, concurrent `cds watch` hung the puppeteer probe navigation for 20 minutes, while the same tree ran 23/23 in 68 s against `cds serve`. CI was never affected because `ci.yml` already uses `cds serve`. First durations ever recorded for the suite: 85 s in CI and 68 s locally for 23 tests on one page; with `--split-opa` the five journey pages take 10/7/22/10/24 s (91 s total). `app/products/package.json` `test:ui` is therefore now `--split-opa --page-timeout 150000 --global-timeout 900000`: a frozen page fails after at most 150 s instead of 900 s and takes one journey down with it instead of the whole tail. The root cause of the 2026-09-09 freeze in `RussianLocaleJourney` case 2 is still unknown and it did not reproduce in any run since.
- Branch: `main` at `bdb6321` (merge of `feature/products-draft-marker`). Since the `products-draft-edit` merge (`c9d9839`) `main` also gained: STATE update after that merge (`c863d76`), the retro of `products-draft-edit` (`8d21809`), protected-file retro follow-ups (`77c47ef`/`e7385e5`), the `@ui5/mcp-server` pin (`d7e5551`/`f786236`), the UI5 MCP plugin-server toggle-off (`08d26a9`/`d6e3912`), the pinned tool-prefix prose fix (`d6560a8`/`151c06a`), and the ADR-0014 MCP audit gate (`5d070aa`/`56169c3`). `main` holds two merged feature runs (`categories-code-list`, `products-draft-edit`), the English pipeline, CI and Dependabot; `products-draft-marker` is merged as of 2026-09-10, so `main` holds three merged feature runs; the retro of that run is on `chore/retro-products-draft-marker`.
- 2026-09-10: the Bash write gap is closed (ADR-0016). The gates stopped trusting the write route: `subagent-stop.mjs` diffs changed files against the ADR-0014 `edit` records and runs the shared per-file-type checks (`scripts/lib/file-checks.mjs`) over whatever was written outside Edit/Write, blocking on a protected path; `stop-gate.mjs` repeats the protected-path audit; the new PreToolUse hook `protect-files-bash.mjs` denies a subagent and asks the main thread. `scripts/lib/**` is protected now, the protected list lives once in `scripts/lib/protected-paths.mjs`, and `test/hooks-protect-bash.test.js` pins the decision table (backend suite 29 tests). The only sanction is `PIPELINE_ALLOW_PROTECTED=1` in the Claude Code process environment: a branch name was tried and rejected, because any agent can create one. Practical consequence for pipeline chores: the user either approves the `ask` prompt or starts the session with that variable.
- Pipeline: stages 0–4 of the plan are implemented and exercised by two full `/feature` runs (`categories-code-list`, `products-draft-edit`); CI and Dependabot are in place. Not done: packaging as a plugin (stage 5).
- Retro follow-ups applied in this branch: `maxTurns` raised (test-ui, ui-verifier, docs-keeper 80; reviewer 50; architect, test-backend 60), `feature` skill updates STATE after every phase, `test-all` gained a dev-server smoke step and a Cyrillic scan, PostToolUse hook flags Cyrillic, `metadata.test.js` checks that `localService/metadata.xml` equals the compiled EDMX (16 tests), CLAUDE.md invariant 10 (English everywhere the AI reads) and a request-to-skill routing table.
- All docs, agent memories, feature files and ADRs are English now; Russian remains only in i18n `ru` bundles, `.texts.csv`, asserted test values and the owner's plan `docs/ai-pipeline-plan.md`.
- Feature `products-draft-edit` merged 2026-09-07 (`c9d9839`): `@odata.draft.enabled` on `CatalogService.Products` (ADR-0012), 22 backend tests, OPA5 17/17 with 0 skipped, browser verification "ready for review" (`docs/features/products-draft-edit/VERIFICATION.md`), review with zero blocking findings, retro triaged (`8d21809`).
- Next feature `catalog-authorization`: specification and ADR-0013 (proposed) are on `main` (`efb05bf`); starts next via `/feature` with the architect's recommended options (Viewer/Editor roles, `@requires` on the service, `@restrict` on `Products`, mocked users alice/bob Editor and a new `viewer`, anonymous 401 incl. `$metadata`), then `liveMode` for the List Report, then the deployment ADR.
- 2026-09-09: `@ui5/mcp-server` pinned in `.mcp.json` (it was started unpinned by the `ui5` plugin, against ADR-0009); STACK.md gained "Setup on a new machine" (plugins are not installed by `enabledPlugins`). Restart check: the project entry does not replace the plugin's server (it registers as `plugin:ui5:ui5-mcp-server`), both ran side by side; agents now reference only `mcp__ui5-mcp-server__*`, and the plugin server is toggled off in `/mcp` per machine (STACK.md setup step 5).
- 2026-09-09: MCP audit (ADR-0014, user decision): hooks log every MCP and skill attempt per agent; SubagentStop asks once for a written `## MCP not used` reason when an edited file type had no query; `manifest.json` is denied for direct edits (Fiori MCP only). Pilot: the marker feature; `/retro` reads `.pipeline/mcp-audit-*.jsonl`. Hooks load at session start: restart before the next `/feature`.
- LESSONS is an inbox (0 pending decisions, 5 pending upstream after the retro of 2026-09-23; the 2026-09-07 "transferred" table was deleted, `docs/CHANGELOG.md` `pipeline` holds the destinations). The retro of 2026-09-10 transferred five entries into rules `tests-ui.md`, `i18n.md`, `ui-annotations.md`, `PATTERNS.md` and the `architect` memory; the sixth became ADR-0016.
- Next steps: the retro of 2026-09-23 is merged (ADR-0017 accepted). Restart the Claude Code session before the next `/feature`, because hooks are snapshotted at session start. Then `liveMode` for the List Report (`docs/STATE.md` open debt, via Fiori MCP), then the deployment ADR (`cds add xsuaa --for production` generates the `CatalogViewer`/`CatalogEditor` role templates and scopes). Follow-ups from the feature stay in open debt: the short `$edmJson` path `/Permissions/isEditor` as the candidate fix for the UI5 console error, and a second OPA5 run as `viewer` if the hidden state ever regresses.

## What works

- Backend on cds 10.0.6, Node 22: `npm run watch` (`cds watch`, port 4004), `npm run lint`, `npm test` (45 tests: 26 in `test/catalog-service.test.js`, 6 in `test/metadata.test.js` incl. the `$metadata` snapshot, 6 in `test/hooks-protect-bash.test.js`, 7 in `test/hooks-registry-gate.test.js`), `npm run docs:registry`
- UI: `npm start` (proxy, :8080) and `npm run start-mock` in `app/products` both open the app from the FLP sandbox; `ui5lint` 0 problems; `npm run lint:js` (Fiori tools ESLint) 0 errors
- Object Page editing of `Products` through drafts (ADR-0012): Edit, Save, Cancel with discard confirmation, Create and Delete on the List Report, Editing Status filter, draft lock across users
- List Report row shows a draft/lock marker (ADR-0015, feature `products-draft-marker`): `Common.SemanticKey: [ name ]` renders a `sap.m.ObjectMarker` in the `Product Name` cell — text-only `Draft` for an own draft, icon-plus-text `LockedBy`/`UnsavedBy` for another user's draft; verified in `en` and `ru`, keyboard-reachable, with the Editing Status filter narrowing to the marked row
- UI tests: `npm run test:ui` in `app/products` (`ui5-test-runner` against `npx cds serve --in-memory --port 4004`, credentials from `app/products/ui5-test-runner.json`): 25 passed, 0 skipped
- Project-level Claude Code plugins: `ui5`, `cap-developer`; MCP in `.mcp.json`: `cds-mcp`, `fiori-mcp`, `chrome-devtools`, `ui5-mcp-server` (pinned 0.2.18 since 2026-09-09; the plugin's unpinned server `plugin:ui5:ui5-mcp-server` is toggled off in `/mcp` on each machine). Setup on a new machine: `docs/architecture/STACK.md`
- Pipeline: 11 subagents in `.claude/agents`, 12 skills, 11 path-based rules, 7 hook events in `.claude/settings.json` (incl. the MCP audit on PostToolUse and PostToolUseFailure), the `docs/registry` registry, the release watcher `scripts/watch-releases.mjs` and the `upstream-check.yml` and `ci.yml` workflows, `.github/dependabot.yml`
- `CatalogService` requires authentication (ADR-0013): logging in as `alice` or `bob` (`CatalogEditor`, browser Basic prompt, empty password) shows Create, Delete and Edit as before; logging in as `viewer` (`CatalogViewer`) shows the same 15-row List Report and Object Page but without those four actions - the read-only singleton `CatalogService.Permissions` drives `UI.CreateHidden`/`UpdateHidden`/`DeleteHidden` in `app/products/annotations/Products.cds`. `npm test` **38 passed**, `npm run test:ui` **25 opaTests, 0 skipped**.

## Open debt

| Item | Resolution | Who |
|---|---|---|
| FLP sandbox still uses the legacy bootstrap (`sandbox.js`, `Container.createRenderer`, deprecated since 1.120, no successor); calls carry `ui5lint-disable` directives | Migrate to the New Sandbox with the `modernize-flp-sandbox` skill from the `ui5-modernization` plugin (needs UI5 >= 1.147; CDN is 1.152) | user decides |
| Keyboard hack for the Explore button in `Component.js` (setTimeout, internal ushell id) | Deliberate decision of the author. Do not touch without a request; the alternative via `CommandExecution` is described in LESSONS | user |
| `Products.price` Decimal(10, 2) instead of the convention Decimal(15, 2) | Kept, ADR-0003. Change at the first model migration | architect |
| `mta.yaml`, `xs-security.json` are drafts without productive dependencies | Separate ADR before any deployment work; `cds add xsuaa --for production` will generate the `CatalogViewer`/`CatalogEditor` role templates and scopes from the CDS role names already in place (ADR-0013) | user |
| `$edmJson` `$Path` through the entity container (`/CatalogService.EntityContainer/Permissions/isEditor`, used by the three `UI.*Hidden` annotations, ADR-0013) trips an unguarded UI5 1.152.0 defect once per page load on :4004: `Failed to read path ... - TypeError: Cannot read properties of undefined (reading '$select')` (`_Helper.aggregateExpandSelect`, `_Helper-dbg.js:238`). Deterministic, identical for both roles, no functional impact measured (`REVIEW.md`, `VERIFICATION.md` scenarios 1-2) | Try the documented fallback short path `/Permissions/isEditor`, regenerate the contract, re-verify in a browser | architect |
| The OPA5 suite for `catalog-authorization` authenticates as one user (`alice`) per run, so the hidden state for a `CatalogViewer` has no automated regression guard; only the backend tests (`@restrict`) and `ui-verifier`'s blocking viewer criterion cover it | Add a second runner config `ui5-test-runner-viewer.json` and a `test:ui:viewer` script only if the hidden state ever regresses (user decision 2026-09-10, PLAN "Open questions") | user |
| CI and Dependabot added 2026-09-07 (`ci.yml`: backend, UI lint, OPA5 journeys; `dependabot.yml`: weekly, grouped, majors of CAP excluded). First run failed (root ESLint picked up the UI config, UI lockfile out of sync), fixed in `8ec176b`; run 34112716975 green: backend, UI lint, OPA5 journeys | Watch Dependabot PRs on Mondays | user |
| `run_manifest_validation` of UI5 MCP 0.2.18 fails with a draft-06 schema error | Workaround via `ui5lint`; wait for a new `@ui5/mcp-server` version via `upstream-check`, then bump the pin in `.mcp.json` | upstream-watcher |
| List Report needs the Go button before the table reloads (FE default, avoids server round trips on every filter change); the user finds it inconvenient for a 15-row catalog | Set `liveMode: true` on `ProductsList` via Fiori MCP `execute_functionality`; tiny feature, needs a PLAN because it changes `manifest.json` and the OPA5 filter journey | user |
| UI tests run only against the live stack (`npm run watch`); the mock (`npm run start-mock`) does not serve `ru` | Known limitation of `sap-fe-mockserver`, no alternative found | |

## Accumulated decisions

See `docs/decisions/`. Key ones: cds 10 + Node 22 (ADR-0001), Vitest + cds-test (ADR-0002), annotations split into semantics in `srv/annotations` and UI in `app/<app>/annotations` (ADR-0004), JavaScript and ESM (ADR-0005), Fiori Elements V4 via Fiori MCP (ADR-0007), mock via `sap-fe-mockserver` (ADR-0008), MCP-first and pinned versions (ADR-0009), `UPPER_SNAKE` code format for own code lists (ADR-0010), the ValueList for associations to a CodeList is generated by the compiler and fixed code lists use a dropdown (ADR-0011), draft editing on `CatalogService.Products` with explicit `IsActiveEntity: true` for non-Fiori clients, no configuration, no handlers (ADR-0012), draft and lock marker in the List Report row via `Common.SemanticKey: [ name ]` on the presentation layer, `name` deliberately left non-unique (ADR-0015); `CatalogService` requires an authenticated user with two roles `CatalogViewer`/`CatalogEditor`, a read-only `Permissions` singleton served by the project's first handler drives role-aware `UI.*Hidden` annotations in the presentation layer, `xs-security.json` deferred (ADR-0013).

## Sessions
- 2026-09-07 15:17 UTC: branch feature/products-draft-edit, changed files 6, compaction auto
- 2026-09-09 10:00 UTC: branch feature/products-draft-marker, changed files 7, compaction manual
