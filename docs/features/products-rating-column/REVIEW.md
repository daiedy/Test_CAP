# products-rating-column: review

Date: 2026-09-25. Reviewer: `reviewer`. Scope: `git diff main...HEAD` (69eb82a plan, f4e1ec7 chore, dc74131 backend, 249a539 ui, dd6ba53 verification). PLAN row 6. Read: PLAN, CONTEXT, SCREENS, VERIFICATION, `research/contract-delta.md`, `research/fe-rating-column.md`.

## Blocking

None.

## Important

- `app/products/webapp/test/integration/data/RatingTexts.js:23` `ru: 'Рейтинг'` is a literal Cyrillic string. The file's own header (lines 8-9) says "Non-ASCII text is written as \u escapes ... to keep the test sources ASCII-only". PLAN row 4 and criterion 24 (ticked), SCREENS "Texts" and the phase 4 CHANGELOG line all say `\u` escapes. `data/CategoryTexts.js` sets the precedent with escapes. The language rule allows Russian in asserted test values (the hook excludes `app/**/webapp/test/**`), so this does not break the language rule. It is still a false statement in committed code and a ticked criterion that is not true. → Replace with `ru: 'Рейтинг', // Reyting` (CONTEXT "Relevant lessons", `CategoryTexts.js` shape). Owner: `test-ui`. Non-blocking: this is a one-line change with no effect on behaviour, and it should be made before the phase 6 commit.

## Minor

- `docs/features/products-rating-column/PLAN.md:22-23` (ticked criteria) still quote the planned calls `iCheckColumns(5, { Rating: ... })`, `iCheckCells(..., { Rating: { value, maxValue } })` and `iCheckField` "with the targetAnnotation identifier". The code uses column key `rating`, cell state `{ editor: { controlType: 'sap.m.RatingIndicator', value, maxValue } }` and field `{ property: 'DataPoint::Rating' }`. PLAN Risks row 2 and SCREENS ("column key expected ... not verified") told `test-ui` to derive the ids from the rendered app, so the change from the plan was allowed. The phase 4 CHANGELOG line records the real ids, and `BaseAPI` not reading `targetAnnotation` is explained in the `RatingTexts.js` header. → Append the actual calls to criteria 22 and 23 and carry the id facts into SUMMARY and the PATTERNS "User scenario" row (DataPoint field identifier). Owner: `docs-keeper`. Non-blocking.
- `PLAN.md:7,19,43,62` and `research/contract-delta.md:41,51` predict a raw phase 3 snapshot diff of +72/-56 with a positional move. The real diff is +16/-0 in both `test/__snapshots__/metadata.test.js.snap` and `metadata.xml`. The net +16 matches, and the content matches the plan: a `UI.DataPoint #Rating` record (7 lines), a LineItem `DataFieldForAnnotation` with `UI.Importance` Low (5 lines) and a GeneralInfo record (4 lines). Phase 2 is +6/-0 as planned. The phase 3 CHANGELOG line already states that the move did not happen. → In SUMMARY, record the actual +22/-0 against `main` and mark the raw figure in criterion 19 as "not observed". `research/` is scratch material, so SUMMARY is the right place for the correction. Owner: `docs-keeper`. Non-blocking.
- `PLAN.md:33` criterion "`npm run lint:js` in `app/products` 0 problems": the fresh run shows 0 errors and 1 warning, `webapp/Component.js:14:17 @sap-ux/fiori-tools/sap-timeout-usage`. That file is protected and unchanged on this branch (not in `git diff main...HEAD`), and the warning comes from the known keyboard hack (CLAUDE.md "Known debt"). `cds lint` and `ui5lint` are clean. → Mark the criterion met, with the known warning named as out of scope. Owner: `docs-keeper`. Non-blocking.
- VERIFICATION note 1: the edit-mode `sap.m.RatingIndicator` clamps a value above `maxValue` and writes it back through the binding. UI5 1.152.0 `sap/m/RatingIndicator-dbg.js` confirms this: `onBeforeRendering` calls `this.setValue(iMVal)` outside the model-to-control update, so the two-way binding PATCHes 5. Only a draft whose out-of-range value was injected through the API can reach this path. The UI cannot produce the value (`maxValue` 5), and active data is guarded by `@assert.range` on direct writes and on `draftActivate`, which the backend tests prove. This is framework behaviour, not a defect of the feature. → Add a `docs/LESSONS.md` entry ("a range check on a RatingIndicator field can only be demonstrated with Save before any re-render") and add a line to SUMMARY. Owner: `docs-keeper`. Non-blocking.
- VERIFICATION note 2 (empty `valuetext`) conflicts with the verifier's own `ru` row, which reads "value text e.g. '5 из 5'". The 1.152.0 source sets `aria-valuetext` from `RATING_VALUEARIATEXT={0} of {1}` in `_updateAriaValues`. The empty value is most likely an artifact of the chrome-devtools snapshot, not an accessibility gap. → Do not record it as debt. At most, SUMMARY lists it as "not verified with a screen reader". Owner: `docs-keeper`. Non-blocking.
- Not executed: mock mode (optional, reason given) and SCREENS scenarios B (sort), F (unrated product, optional) and G (narrow width, optional). The PLAN allows the mock-mode skip with a stated reason. B and G are informational. For F, the 1.152.0 source shows `setValue(null)` resolves to the default 0 inside the model update, and `onBeforeRendering` only corrects values above max or below 0, so no write-back of 0 for an unrated product is expected. That is read from the source, not tested. → List all four as "not verified" in SUMMARY. Owner: `docs-keeper`. Non-blocking.
- `docs/CHANGELOG.md:11` "- feature `products-rating-column` (#5), phase 2 in progress: ..." has no scope prefix (`db`/`srv`), ends with a trailing space, and its "in progress" is now stale. `docs/STATE.md:5-6` has a doubled blank line before `## Now`, and "Last commit" is 249a539 rather than dd6ba53. `What works` still shows 57/25 tests (the correct figures are 69/28), which PLAN row 7 already schedules. → Fix these in phase 6. Owner: `docs-keeper`. Non-blocking.
- `app/products/webapp/test/integration/RussianLocaleJourney.js` fails `npx prettier --check`. The `main` version fails the same check, so the branch did not introduce this. The feature changed only the `sap.ui.define` header and two lines. → Optional `npx prettier --write` in a later chore. Owner: `test-ui`. Non-blocking.

## Checked and in order

- Blocking gate: zero blocking findings.
- Layers: `db/schema.cds` gets only `rating : Integer;`, and `grep` finds no `@UI`/`@title`/`@assert` in `db/`. Semantics (`@title`, `@assert.range: [0, 5]`) are in `srv/annotations/Products.cds`, and `srv/` has no `@UI.`. `UI.DataPoint #Rating` and both `DataFieldForAnnotation` records are in `app/products/annotations/Products.cds`, in the existing block, with no second LineItem. No handler was added and `srv/catalog-service.js` is untouched, which matches "declarative before imperative".
- Plan conformance: the recommended defaults were implemented, and rows 2b, 3b and 3c were not executed. Positions match CONTEXT/SCREENS: LineItem 5th and last with `#Low`, GeneralInfo 4th between `category_code` and `imageUrl`, no `Title`/`Description`, no `HeaderFacets`. The CSV is still `;`-separated, has 15 rows, and has `Laptop Pro 15`=5, `Yoga Mat`=4, one 0 and one 1. `mockdata/Products.json` has the same 15 values as the CSV. `git diff --stat main...HEAD -- app/products/webapp/manifest.json` is empty.
- PATTERNS rows: "New entity" (element + CSV), "Format or range check", "Table columns, filters, header, sections" (the first `UI.DataPoint`; amending the row is scheduled for phase 6 and needs no ADR per PLAN), "Texts" (`_i18n` key `Products.rating`, as for the existing labels), "Service test", "OData contract", "metadata.xml snapshot update" (both in their own phases), "User scenario", "UI without backend". No decision in the diff lacks a row.
- Registry and duplicates: no new function, handler, fragment, formatter or type. `search_model` on `CatalogService.Products` shows `rating` with `@Common.Label`, `@Validation.Minimum` 0 and `@Validation.Maximum` 5. `RatingTexts.js` is the planned sibling of `CategoryTexts.js`, and `RatingShownAsStarsJourney.js` is a new journey, not a copy of an existing one.
- i18n: `Products.rating` exists in both `_i18n/i18n.properties` (Rating) and `_i18n/i18n_ru.properties`, with an `#XFLD` comment. The `ru` header is asserted by OPA5 and was seen by the verifier.
- Tests against criteria: the four backend tests match criteria 13-16. The draft test's final `rating: null` is correct because the `beforeAll` product "Draft Lamp" has no rating. The contract test pins the DataPoint record and scopes both `DataFieldForAnnotation` records to LineItem and GeneralInfo (the phase 3 report says it was proven red on the phase 2 model). OPA5 asserts the control type and state, not text.
- Contract content: the snapshot and `metadata.xml` diffs against `main` are both +22/-0 and hold only the rating property, its Label/Minimum/Maximum, the DataPoint, and the two records.
- Language: no Cyrillic in scope outside `_i18n/*_ru.properties`, `VERIFICATION.md` (quoted rendered UI, which is allowed) and test values.
- `f4e1ec7` chore: a one-line `eslint-disable-next-line no-control-regex` with a reason in `scripts/hooks/session-start.mjs`, approved by the user, in its own commit, with its own `pipeline` CHANGELOG line. It is outside the feature scope but separated from it, so it is not a finding.
- Fresh runs by the reviewer, 2026-09-25: `npm run lint` (cds lint) clean; `npm test` 7 files, **69 passed**; `node scripts/check-docs-fresh.mjs` "docs/registry is fresh."; `node scripts/check-feature-docs.mjs products-rating-column` passes; `diff <(cds compile '*' --to edmx-v4 -s CatalogService -l en) metadata.xml` shows no difference; `ui5lint` "No findings detected"; `npm run lint:js` 0 errors, 1 warning (in the protected `Component.js`, see above); `prettier --check` clean on the new and changed test files except the pre-existing `RussianLocaleJourney.js`. The 28/28 OPA5 run was not repeated here: it rests on the phase 4 report and the CHANGELOG, because the runner output is gitignored.

## Verdict

ready to commit. There are no blocking findings. The one Important item is a one-line fix for `test-ui` before the phase 6 commit, and the Minor items belong to `docs-keeper` in phase 6.
